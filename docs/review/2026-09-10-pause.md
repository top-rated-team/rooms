# Where this stands, 10 September 2026

Written at the owner's instruction, on pause until Monday. Everything below is
pushed to `main`; nothing is waiting in a working tree.

## Live and verified

- **adgrant.ai** serves the AdGrant tree at its root with its own title,
  description, favicon and preview card. `top-rated.team/adgrant/*` is a 301 to
  it. 33 addresses in its own generated sitemap.
- **The hidden-agent gate.** `/api/ask` and every other route refuse the
  withheld LinkedIn agents with the same reply as for an agent that never
  existed. `server/hidden-agents.test.ts` fails loudly if nothing is hidden, so
  this cannot be half-undone in silence.
- **Waves 16 and 17 are in**, both after review and repair — 574 tests.

## Credentials set on Render this session

`LINKEDIN_CLIENT_ID`, `LINKEDIN_CLIENT_SECRET`,
`GOOGLE_FREEBUSY_CLIENT_ID`, `GOOGLE_FREEBUSY_CLIENT_SECRET`,
and earlier `RESEND_API_KEY` / `LEAD_EMAIL_FROM`.

**Untested end to end:** the LinkedIn buttons. My probe of the three redirect
URLs could not tell an unregistered redirect from an unaccepted scope — the
same page came back for a deliberately wrong URL — so the only real check is
pressing the button once. If it bounces off a LinkedIn error page, the URLs are
not registered on the app the client id belongs to.

## Waiting to run, in order

| # | Parcel | Note |
| --- | --- | --- |
| 18 | `room-account` | Logout, and one account with several ways in. Introduces a session cookie and an account merge — the merge is the dangerous part and its test comes first. |
| 19 | `visitor-calendar` | Its two credentials are now set. Ask for `calendar.freebusy` only; the owner may delete the other three scopes from the app. |
| 20 | `upwork-promo-video` + `case-studies-doc` | Two prompts, one wave: neither touches a seam so they cannot collide. |

## Not done, and why

- **The Upwork promo video.** Never made, and I did not say so for too long.
  `ffmpeg` and the CDP harness are both on this machine, so wave 20 renders it
  from a script rather than by hand — a video edited by hand goes stale the day
  a price changes.
- **A demo video for Google's OAuth verification** cannot be recorded until
  wave 19 exists: there is no flow to film. That is the same blocker, not a
  second one.
- **The Google Doc copy of the case studies** needs a direction decided before
  it is built — see `case-studies-doc`. A two-way sync that quietly loses an
  afternoon of somebody's edits is worse than an honest one-way export.

## Owed, from the last message

All four done and pushed: the mark removed from the subtitle, the doubled rule
in the hover, and both popups — Contact and Book a call — moved onto the site's
own actions and underline fields.

## The thing to keep doing

Every wave so far has come back with real defects, and three of them shipped
the same shape: state in a process-local Map that a deploy silently erases.
Review before commit, and look for that one first.
