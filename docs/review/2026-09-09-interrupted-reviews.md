# Where the reviews got to when the session limit hit

Written 9 September 2026. Two adversarial review runs were cut off mid-flight by
an account session limit. Their raw output is here so that resuming does not mean
re-running them. Neither run's summary line should be believed over this file:
the wave-13 run reported "refuted: 32" and in fact **zero** of its verifiers
completed, so those 32 claims are UNVERIFIED, not refuted.

Both runs can be resumed rather than repeated — completed agents replay from
cache. Run ids and scripts:

| run | script | resume id |
| --- | --- | --- |
| wave 13 review | `~/.claude/projects/.../workflows/scripts/review-wave-13-wf_1b4af52f-463.js` | `wf_1b4af52f-463` |
| LinkedIn sweep | `~/.claude/projects/.../workflows/scripts/linkedin-hide-sweep-wf_21638c18-ce4.js` | `wf_21638c18-ce4` |

---

## 1. room-login (wave 13) — 32 claims, NONE verified

This parcel deleted the security seal on `/api/room-access`. It must not be
deployed until these are either fixed or refuted. Grouped by file.

### `client/src/components/site/RoomMenu.tsx`

- **line 116** · _untrue-comment_ — The component docstring says "With rooms remembered it stays Your rooms | Open a room" and the render agrees, but the brief says that state is gone and LOGIN must lead in every state — with the result that the Login button, and therefore the entire login panel this parcel builds, is unreachable for any browser that remembers a room.
  <br>Failure: The `{testId}-login-open` button exists only inside the `rooms.length === 0` arm (lines 477-490); `openLogin()` has no other caller. A returning visitor whose localStorage still holds one stale room — the exact person the brief names as the one who needs LOGIN — sees "Your rooms | Open a room", hovers to get a list containing only the stale room, and has no way to reach LinkedIn, WhatsApp or the mailed link short of clearing site data. The docstring cements the behaviour the brief calls removed, so a later reader will treat it as intended.
- **line 190** · _untested-branch_ — The WhatsApp poll handles 0 and 1 rooms explicitly and then, for an identity with several rooms, silently navigates into `rooms[0]` with no picker and no notice — untested, and inconsistent with the LinkedIn path, which serves loginPickerPage for the same case.
  <br>Failure: A person has bound two rooms from one WhatsApp chat. They press WhatsApp, send the code, and the page jumps into whichever room came first out of the `byPerson` Set iteration in identity-store.ts:154-158 — an arbitrary order, not "most recent" or anything a visitor could predict — with no way to reach the other. Neither this branch, nor loginPickerPage, nor the two-room branch of completeRoomLoginLinkedIn, nor emailBody's plural "Each of these links" text has any test; the whole many-rooms case is unexercised. Note there are no route-level tests either: /api/room-login, the LinkedIn callback's 0/1/many split and POST /api/workspaces/:token/room-address have no routes.*.test.ts, though the repo has that convention for other route groups.
- **line 529** · _correctness_ — The brief's "the 'Your rooms' state is gone… LOGIN | Open a room, in that order, always" is not implemented: with a remembered room the control still renders "Your rooms | Open a room" and there is no LOGIN button at all, so a returning visitor with any remembered room cannot reach LinkedIn, WhatsApp or the address form.
  <br>Failure: A visitor who has ever opened a room on this browser has a non-empty localStorage rooms list, so rooms.length > 0 and the component takes the `newest` branch at line 505, which renders a <Link>"Your rooms"</Link> and "Open a room" and never renders the `${testId}-login-open` button that is the only thing calling openLogin(). loginOpen stays false forever, dropdownPanel renders only `list`, and the login panel is unreachable. That visitor sits at a laptop whose localStorage still remembers a stale room but who needs to log in to reach a DIFFERENT room on another device — the exact person the brief says LOGIN is first for — and the site offers no way in. The component docstring at lines 116-117 states the shipped behaviour ("With rooms remembered it stays Your rooms | Open a room"), which contradicts the brief's instruction that the state is gone.
- **line 529** · _brief-fidelity_ — The "Your rooms" state the brief deleted is still rendered, and that branch contains no LOGIN control, so the entire login panel is unreachable in any browser that remembers a room.
  <br>Failure: A visitor who has ever opened a room has entries under ROOMS_STORAGE_KEY, so `rooms.length === 0` is false and the component renders the second branch: `Your rooms | Open a room`. That branch has no `${testId}-login-open` button — the only caller of `openLogin()` — so LinkedIn, WhatsApp and the address link can never be reached by the returning visitor the feature exists for; only a visitor with empty localStorage ever sees LOGIN. The brief says "THE CONTROL IS ONE PAIR IN EVERY STATE: LOGIN | Open a room, in that order, always" and "THE 'Your rooms' STATE IS GONE, on the owner's instruction: hovering LOGIN shows the remembered-room list when this browser has one". The component's own doc comment at lines 116-117 ("With rooms remembered it stays Your rooms | Open a room") states the violated shape as though it were the specification.

### `server/room-access.test.ts`

- **line 62** · _test-coverage_ — `useMemoryRoomAccessForTests()` in beforeEach (and the same call in room-login.test.ts:75) makes durableConfigured() true and persistBinding/persistLink return true without touching a database, so every line added for the durability repair has zero coverage.
  <br>Failure: With memoryDurableForTests set, `persistLink` returns at line 445 before `getDb()`, `hydrateFromDb` returns at line 144 because hasDb() is false, and markLinkSpent returns at line 546. Delete the bodies of the insert, the hydrate loop and the spent-update — reducing the whole feature back to the process-local Maps the seal condemned — and the entire suite still passes. Concretely untested: the roomAccessLinks insert, the roomAddressBindings insert/update, the hydrate-on-boot loop including its `expiresAt <= now` skip, the mark-spent update, and every catch block that logs and returns false.
- **line 151** · _stub-passing-test_ — The timing-equalisation test measures no time at all — it records sleep arguments through a no-op and asserts only "some sleep > 0" and "every sleep ≤ 800", which an equalizeTiming that always slept 1 ms would satisfy.
  <br>Failure: Replace the body of equalizeTiming with `await sleepImpl(1)` and this test still passes. It also never exercises the found path, so nothing compares the two: `lastSendMs` (room-access.ts:69) is a module global seeded at TIMING_FLOOR_MS, so the first bound-address send after a boot returns after however long Resend took — up to the 8 s EMAIL_TIMEOUT_MS — while an unbound address returns at 800 ms. A caller timing two requests learns which address has a room, and the suite reports the repair as done.
- **line 161** · _vacuous-test_ — The regression guard for the defect that sealed these routes — "does not treat workspaces.visitorEmail as a binding" — never creates a workspace whose visitorEmail is ADDRESS, so it passes for any implementation, including one that reads that field.
  <br>Failure: The test body is only `sendRoomAccessLink({email: ADDRESS})` + `assert.equal(posted.length, 0)`; no workspace with `visitorEmail === "ada@example.test"` exists in that test (the only `storage.createWorkspace` in the file is 55 lines later and passes no visitorEmail). Re-add `storage.getWorkspacesByVisitorEmail(email)` to the room lookup in sendRoomAccessLink and `npm test` stays green — the exact defect the 404 seal was written for ships again with a green suite. A real guard costs three lines: `await storage.createWorkspace({name:"attacker", visitorEmail: ADDRESS})`, then send, then assert nothing was posted.

### `server/room-access.ts`

- **line 11** · _untrue-comment_ — The file header states "THE REPLY ON SCREEN IS THE SAME whether or not a room was found", but the provider-reject and persist-failure paths return a different status and a different line, and both are reachable only when a room was found.
  <br>Failure: Resend is rejecting (expired key, suspended sending domain, 8 s timeout). POST /api/room-access with a bound address returns 503 + "The link could not be sent. Try again."; with an unbound address it returns 200 + "If that address has a room, the link is on its way." RoomMenu.tsx:242-259 renders both verbatim, so during any provider outage the form is a customer-enumeration oracle — the precise thing the header, the routes comment and the brief all forbid. The same split exists on the persistLink failure at line 516. The docstring at lines 468-473 quietly admits the exception; the header comment fourteen lines from the top of the file does not.
- **line 78** · _security_ — Defect 2 is not fixed on the deployment as configured: rows are durable but their keys are not — with ROOM_HASH_PEPPER unset the pepper is randomBytes(32) per process, so after a restart every stored tokenHash and emailHash is unreachable and a live link says "already been used".
  <br>Failure: render.yaml does not set ROOM_HASH_PEPPER (it lists PUBLIC_BASE_URL, DATABASE_URL, RESEND_API_KEY, LEAD_EMAIL_FROM and others, not this one) and autoDeploy is true, so the house process restarts on every push. Sequence: a visitor asks for a link at 10:00; persistLink writes a row keyed by hashAccessToken(token) under pepper P1. A deploy lands at 10:20; the new process generates P2 at first digest() call. The visitor clicks the mailed link at 10:30 — well inside the hour the mail and ROOM_ACCESS_SPENT_LINE both promise. hydrateFromDb loads the row into `links` under its P1 key; openRoomAccess computes hashAccessToken(token) under P2 and misses, and the visitor is shown "This link has already been used, or an hour has passed". That is defect 2's exact symptom. The same applies to every address binding: roomsForEmail does bindings.get(hashEmail(email)) under P2 against rows keyed under P1, so after any restart the feature silently finds nothing and answers with the same reassuring ROOM_ACCESS_SENT_LINE. roomAccessAvailability() (line 232) gates on hasDb() and configuredPublicBaseUrl() but never on the pepper, so the feature reports itself available and goes on promising the hour. identity.ts:141-150 already documents this exact hazard for its own pepper and at least logs a warning at boot; this file has neither the warning nor the availability check.
- **line 78** · _false-promise_ — hashPepper() falls back to a per-process `randomBytes(32)` and roomAccessAvailability() never checks ROOM_HASH_PEPPER, so a deployment with DATABASE_URL and no pepper still advertises itself as available and still promises "one hour" that a restart voids — the sealed defect (b), unrepaired for the default configuration.
  <br>Failure: Set DATABASE_URL, leave ROOM_HASH_PEPPER unset (nothing in render.yaml, README or the availability check requires it). A visitor is mailed a link; the row is persisted with `tokenHash = sha256(randomPepper ‖ …)`. The process restarts nine minutes later; hydrateFromDb loads the row, but hashAccessToken now derives from a *different* random pepper, so `links.get(hashAccessToken(token))` misses and openRoomAccess answers ROOM_ACCESS_SPENT_LINE — "This link has already been used, or one hour has passed" — to a link that is neither. No test restarts the module, and resetRoomAccessForTests nulls the pepper anyway, so nothing in the suite can see it.
- **line 310** · _security_ — The identical-timing requirement is only partly met: equalizeTiming pads to the duration of the Resend call alone, so the found path additionally carries the persistLink database round trip that the not-found path never pays, leaving a measurable oracle for whether an address is a customer here.
  <br>Failure: On the found path the clock from startedAt covers persistLink (a Neon serverless INSERT, tens to hundreds of ms, and far more on a cold connection) plus sendViaResend, and lastSendMs is set to the Resend duration only; equalizeTiming then computes max(TIMING_FLOOR_MS, lastSendMs) - elapsed, which is already negative, so it sleeps zero and the response lands at dbInsert + resend. On the not-found path elapsed is ~0 and the response lands at exactly max(800, lastSendMs) = the previous resend duration. An attacker samples POST /api/room-access for a known-bound address he created himself (finding 1) to learn the baseline, then samples a target address: a consistent extra ~40-200ms on every probe says the target has a room here. On a freshly restarted process the gap is larger still — lastSendMs starts at TIMING_FLOOR_MS 800, so the first not-found reply returns at 800ms while the first found reply returns at dbInsert + a real 1200ms Resend call.
- **line 310** · _identical-reply_ — `equalizeTiming` pads only to the previous send's duration, and `lastSendMs` is a process-global mutated only when a provider call actually happened, so response timing still discloses whether a room was found.
  <br>Failure: On the found path `sendViaResend` sets `lastSendMs` to its own duration before `equalizeTiming` runs, so the subtraction leaves nothing to wait for and the response takes the real Resend latency; the no-room path returns after `max(800ms, lastSendMs)`. An attacker brackets a probe with two controls: POST a known-unbound address (t0), POST the target, POST the known-unbound address again (t1). `lastSendMs` changes only if the middle request performed a real send, so t1 differs from t0 exactly when the target address has a room — independent of how fast or slow the provider happens to be that minute. The plain version is visible too: any send slower than the 800 ms floor (a p95 Resend call) returns visibly later than the not-found reply issued moments earlier.
- **line 392** · _correctness_ — bindRoomAddress still has no reachable production caller for a genuine customer — the client never posts to /api/workspaces/:token/room-address, and the only other caller (LinkedIn login) requires a person who can already get into the room — so the address way still "cannot find anything", which the brief names as the reason it was written.
  <br>Failure: grep of client/src finds exactly four calls into these APIs (RoomMenu.tsx:173, 210, 236, 353) and none of them is POST /api/workspaces/:token/room-address; no workspace page renders an "add your address to this room" field. The only other bindRoomAddress caller is completeRoomLoginLinkedIn (room-login.ts:242), which binds the LinkedIn email only to rooms already returned by listBindingsByPerson("linkedin", sub) — i.e. rooms the person had already bound LinkedIn to from inside, in which case LinkedIn login alone already gets them in. So a real customer who cleared their browser types their address into the LOGIN panel, gets the reassuring ROOM_ACCESS_SENT_LINE, and no mail ever arrives, because nothing in the shipped product ever wrote a binding for them. The only rows that reach room_address_bindings in practice are the ones an attacker writes by hand via the unauthenticated endpoint in finding 1.
- **line 400** · _authorisation_ — bindRoomAddressForToken binds any address a caller types to a room the caller merely holds a token for, with no proof of address control — recreating the self-asserted-email defect the seal was written for, only through a different field name.
  <br>Failure: Sealed defect 1 said: "an anonymous caller opens a room, sets visitorEmail to a stranger's address, and asks us to mail that stranger a link into the attacker's room." The same chain works today in three unauthenticated calls. (1) POST /api/workspaces with an empty body — no auth, createWorkspaceLimit 10/hr — returns `created.token` (server/routes.ts:683). (2) POST /api/workspaces/<that token>/room-address with {"email":"victim@corp.com"} — requireWorkspace (server/routes.ts:200-210) checks only that the token resolves to a workspace, which it does because the attacker just minted it, so bindRoomAddress writes hashEmail("victim@corp.com") -> {attacker workspaceId, attacker workspaceToken} into room_address_bindings. Nothing anywhere asks the holder of victim@corp.com whether they consent. (3) POST /api/room-access with {"email":"victim@corp.com"} — roomsForEmail finds that binding, mints a live one-hour link, and Resend mails it from LEAD_EMAIL_FROM with the subject "A link to your room". The victim receives, from the house domain and the house sender, a working link that opens the attacker's room, where the attacker reads everything they type. The file header at server/room-access.ts:16-17 ("An address earns a room only through bindRoomAddress") and the doc comment at 387-391 ("this is not a way to bind a stranger's address to a room you do not have") are both literally true and both miss the attack, which binds a stranger's address to a room the attacker DOES have. The seal on /api/room-access was deleted in this same commit, so this is live.
- **line 516** · _identical-reply_ — The room-found path can answer 503 with a different line while the no-room path always answers 200 "sent", so the reply is not identical whether or not a room was found.
  <br>Failure: DATABASE_URL is set but `npm run db:push` has not yet created `room_access_links` — precisely the state identity-store.ts:7 and room-access.ts:186 warn about. `roomAccessAvailability()` still reports available (it only checks `hasDb()`), so the form is drawn; then every POST /api/room-access for an address that HAS a bound room reaches `persistLink`, the insert throws, and the caller gets 503 "The link could not be sent. Try again.", while every address with no room gets 200 with ROOM_ACCESS_SENT_LINE. One POST per address then enumerates exactly which people are customers here — the disclosure the identical-reply rule exists to prevent. The same asymmetry exists on the provider-rejects branch at line 529, which only a found address can ever reach.

### `server/room-login.test.ts`

- **line 151** · _unreachable-key_ — Every "rooms found" assertion resolves its token from `registerRoomTokenForTests`, a Map that no production path ever writes, so tokensFor's real workspaces query is untested — and that query bypasses `storage`, which is the only workspace store on a deployment without DATABASE_URL.
  <br>Failure: tokensFor (room-login.ts:157-179) reads `testTokens` first, then returns early at line 163 when `!hasDb()`. Delete the drizzle query entirely and all four rooms-found assertions in this file still pass, because the token they check came from the test-only Map. In production without DATABASE_URL — the mode server/db.ts:20-22 and server/storage.ts:631 document as supported, where MemoryStorage holds real workspaces — tokensFor can never return a token, so roomsFromBindings always yields `[]`, and routes.ts:1359-1360 serves a person whose room is genuinely bound the sentence "No room is bound to this LinkedIn account." That sentence is false, and roomLoginAvailability still offers LinkedIn on that deployment even though the email way correctly reports itself unavailable for want of the same database.
- **line 219** · _tautological-test_ — Both WhatsApp tests plant the binding with `providerId: whatsappProviderId(CHAT)` — the very function under test — so the lookup key is generated by the code being verified and can never disagree with it.
  <br>Failure: proveRoomLoginWhatsApp is only ever asked to find a row that whatsappProviderId itself wrote, so the assertion `rooms: [{token: ROOM_TOKEN}]` holds for any self-consistent hash — including the empty-pepper variant that does not match what identity.ts:531-533 actually stores. Change the separator, the digest, or the pepper fallback in whatsappProviderId and both tests still pass while WhatsApp login stops finding any real binding. The test that would catch it writes the binding through identity.ts's own bind path (or asserts the two hex strings are equal with the pepper deleted), which nothing here does.

### `server/room-login.ts`

- **line 79** · _correctness_ — WhatsApp login can never match a binding on the house deployment: whatsappProviderId falls back to an EMPTY pepper while identity.ts falls back to randomBytes(32), so the two hashes of the same chatId never agree, and the comment above claims the mismatch only happens "after a restart".
  <br>Failure: ROOM_HASH_PEPPER is not set in render.yaml. identity.ts:145-152 therefore sets HASH_PEPPER = randomBytes(32) and opaqueWhatsAppId (identity.ts:532) writes providerId = `whatsapp:` + sha256(randomBytes32 ‖ 0x00 ‖ chatId) into room_bindings. room-login.ts:79 uses Buffer.alloc(0) for the same fallback, so whatsappProviderId(chatId) computes sha256(empty ‖ 0x00 ‖ chatId). The owner opens LOGIN on top-rated.team, presses WhatsApp, sends the prefilled "Room-login ABCD…" message from a chat that IS bound to one of his rooms; proveRoomLoginWhatsApp calls listBindingsByPerson("whatsapp", <empty-pepper hash>), the reverse index holds only <random-pepper hash>, zero rows come back, and the panel shows "No room is bound to this WhatsApp chat" forever — not after a restart, but on the very first attempt within a single process. The docstring at lines 74-76 ("Without the pepper the hash will not match a binding written by identity.ts after a restart") states something weaker than what the code does and so is untrue of the code as it stands.
- **line 79** · _correctness_ — whatsappProviderId falls back to `Buffer.alloc(0)` when ROOM_HASH_PEPPER is unset while identity.ts's opaqueWhatsAppId falls back to `randomBytes(32)`, so the two hashes never agree on the deployment the docstring above claims they do.
  <br>Failure: identity.ts:145-152 computes `HASH_PEPPER = configured ?? randomBytes(32)` once at module load and writes `whatsapp:sha256(HASH_PEPPER ‖ 0x00 ‖ chatId)`; room-login.ts:78-81 computes `configured ?? Buffer.alloc(0)`. On any deployment that has not set ROOM_HASH_PEPPER — the documented degraded default, identity.ts even logs it at boot — a visitor binds a room over WhatsApp and then presses LOGIN → WhatsApp thirty seconds later in the *same process*: listBindingsByPerson is queried with a hash computed from an empty pepper, matches nothing, and the panel says "No room is bound to this WhatsApp chat." The docstring at lines 72-77 asserts the opposite twice: "Same formula identity.ts uses" and that a mismatch happens only "after a restart".
- **line 79** · _correctness_ — whatsappProviderId falls back to an empty pepper while identity.ts falls back to randomBytes(32), so with ROOM_HASH_PEPPER unset the login hash never equals the binding hash and the comment above it is untrue.
  <br>Failure: identity.ts:145-152 builds HASH_PEPPER as Buffer.from(ROOM_HASH_PEPPER) if set, else randomBytes(32), and writes providerId = `whatsapp:sha256(HASH_PEPPER ‖ 0x00 ‖ chatId)` (identity.ts:531-532, 641). room-login.ts:79 uses Buffer.from(ROOM_HASH_PEPPER) if set, else Buffer.alloc(0). ROOM_HASH_PEPPER is unset by default — identity.ts:148 logs exactly that at boot — so on the deployment as it runs today the binding is keyed on sha256(random32 ‖ 0 ‖ chatId) and the login looks up sha256("" ‖ 0 ‖ chatId). Concrete: a customer binds their room over WhatsApp at 10:00, then at 10:05 in the SAME process presses LOGIN and messages "Room-login ..." from the same chat. listBindingsByPerson returns [], whatsappResults stores zero rooms, and RoomMenu.tsx:187 tells them "No room is bound to this WhatsApp chat." WhatsApp login is dead on every deployment that has not set the pepper, and it fails silently rather than reporting itself unavailable. The comment at room-login.ts:72-76 claims two things that are false of this code: that it is the "same formula identity.ts uses" (identity.ts's default is 32 random bytes, not zero bytes), and that the mismatch only appears "after a restart" (it is there from the first request).
- **line 79** · _brief-fidelity_ — `whatsappProviderId` falls back to an EMPTY pepper when ROOM_HASH_PEPPER is unset while identity.ts falls back to `randomBytes(32)`, so the reverse lookup can never match a WhatsApp binding on a deployment that has not set that variable.
  <br>Failure: render.yaml lists no ROOM_HASH_PEPPER, and identity.ts:148-153 logs "no ROOM_HASH_PEPPER" at boot and uses a per-process `randomBytes(32)` for `opaqueWhatsAppId` (identity.ts:532). This function instead uses `Buffer.alloc(0)`. A visitor whose room is bound over WhatsApp presses LOGIN → WhatsApp, sends "Room-login ABCDEFGHJKLM" from the phone that bound it; `proveRoomLoginWhatsApp` hashes the same chatId with an empty pepper, `listBindingsByPerson` finds nothing, and the panel prints "No room is bound to this WhatsApp chat" for a chat that is bound — every time, on every deployment without the pepper, so the way is drawn and can only fail. The comment at lines 74-76 ("Without the pepper the hash will not match a binding written by identity.ts after a restart") is false in the direction that matters: without the pepper it never matches, including inside the same process, which is also why no test catches it — server/room-login.test.ts writes its bindings with this file's own `whatsappProviderId` rather than identity.ts's.
- **line 134** · _availability_ — roomLoginAvailability advertises LinkedIn and WhatsApp as working ways in without checking that rooms are stored in a database, but tokensFor can only resolve a room token through the database, so both ways always answer "no room" on a no-database deployment.
  <br>Failure: tokensFor bails at server/room-login.ts:163 (`if (ids.length === 0 || !hasDb()) return result;`) — outside of tests the only source of a room token is a SELECT against the `workspaces` table. Without DATABASE_URL the app runs on MemoryStorage (server/storage.ts:626-633, boot log "in-memory — no DATABASE_URL") and identity-store keeps bindings in a Map (its own header says that is "how the product runs today"), so tokensFor returns an empty map, roomsFromBindings and completeRoomLoginLinkedIn drop every binding via `if (!token) continue`, and the visitor is told "No room is bound to this LinkedIn account" (server/routes.ts:1379) or "No room is bound to this WhatsApp chat" even when the binding was written a minute earlier in the same process. Meanwhile the panel still draws both buttons, because linkedin is decided only on linkedinConfigured() && publicUrl (line 133-134) and whatsapp only on isHouseHost + the WAHA probe (lines 143-148). The email way gets this right — roomAccessAvailability returns ROOM_ACCESS_NO_DATABASE_LINE when !durableConfigured() (server/room-access.ts:233) — so on the same request the panel truthfully hides the address form and untruthfully offers the two ways that cannot work, which is precisely the "never a way that does not" rule the brief sets.
- **line 143** · _fork-safety_ — The house/fork gate is `isHouseHost`, which returns true for any `*.onrender.com` host, so a fork deployed from this repo's own render.yaml blueprint is classified as house and is drawn and served the WhatsApp way.
  <br>Failure: Someone forks the public repo and points Render at the committed render.yaml; the service comes up as e.g. `top-rated-team-a1b2.onrender.com`. `isHouseHost` (shared/operator.ts:139) returns true for that suffix, so BOTH gates pass: the browser gate `isHouseHost(window.location.hostname)` at client/src/components/site/RoomMenu.tsx:326 is true, and this server gate is true, so GET /api/room-login returns `whatsapp:{available:true}` as soon as the fork has any Unipile account configured. The panel draws the third way and GET /api/room-login/whatsapp answers with a wa.me URL plus a QR of it. When that fork's Unipile account body carries no phone number, probeWaha falls back to `publishedDigits()` (server/waha.ts:123), which parses shared/roster.ts WHATSAPP_URL — so a fork hands its own visitors the owner's personal mobile, in a link and in a scannable QR. The file comment at lines 9-11 asserting the decision "a fork cannot reach around" is therefore untrue, and the parcel's only fork test uses host "partner.example" (server/room-login.test.ts:97), never an onrender or preview host.
- **line 163** · _brief-fidelity_ — `tokensFor` resolves room tokens only out of the `workspaces` table, so with no DATABASE_URL both LinkedIn and WhatsApp login always return zero rooms, yet `roomLoginAvailability` still advertises both as ways in.
  <br>Failure: render.yaml marks DATABASE_URL `sync:false` ("Leave empty and rooms live in memory") and server/identity-store.ts:5 says memory is "how the product runs today". In that configuration rooms are in MemStorage and a binding sits in the in-memory `byPerson` index, but `hasDb()` is false, so `tokensFor` returns an empty map and both `completeRoomLoginLinkedIn` and `roomsFromBindings` produce `rooms: []`. LOGIN nonetheless draws LinkedIn (availability checks only `linkedinConfigured()` and PUBLIC_BASE_URL, lines 133-141) and WhatsApp on the house host, and every completed sign-in lands on "No room is bound to this LinkedIn account" — while the one way that does report itself unavailable without a database, the address link, is the only one hidden. The brief requires the panel to offer "only the ways that actually work on THIS deployment and never a way that does not".
- **line 232** · _false-statement_ — Every LinkedIn failure — expired state, user-cancelled consent, a failed token exchange — is reported to the visitor as "No room is bound to this account.", which is a claim the code has not established.
  <br>Failure: ROOM_LOGIN_NONE_LINE is returned for a missing state (line 221), an unknown or swept state (line 224), LinkedIn returning error=access_denied or no code (line 225), and a failed token exchange or userinfo read (line 232), and server/routes.ts:1370-1372 renders it as a 404 page. None of those paths ever looked at a binding. A returning customer who leaves the LinkedIn consent screen open past PENDING_MS (15 minutes), or who hits a LinkedIn 5xx during the exchange, is told their room does not exist — a statement about their account that the server cannot support, and one that sends them away instead of telling them to press LOGIN again. It also disagrees with the genuine zero-rooms answer, which is a 200 with a different sentence (server/routes.ts:1379), so the two cases the visitor most needs told apart are the two that read the same.
- **line 241** · _authorisation_ — completeRoomLoginLinkedIn binds the userinfo email to the caller's rooms without consulting email_verified, so an unverified LinkedIn email claim becomes a room-earning address.
  <br>Failure: identity.ts declares `email_verified?: unknown` on the UserInfo interface (line 350) and its own doc at line 88 names it, but emailFromUserInfo (identity.ts:375-380) only shape-checks the string and nothing anywhere reads the flag. So whatever address LinkedIn's userinfo returns is written straight into room_address_bindings by bindRoomAddress at server/room-login.ts:242-246, becoming a caller the address way trusts. An attacker whose LinkedIn account carries an unverified email claim for victim@corp.com signs in through GET /api/room-login/linkedin, the binding victim@corp.com -> attacker's room is written, and the attacker then POSTs {"email":"victim@corp.com"} to /api/room-access — the house mails the victim a working one-hour link into the attacker's room. That is the outcome the seal existed to prevent, and the brief's instruction that bindRoomAddress take "the LinkedIn userinfo email" as a real caller is satisfied only for an email LinkedIn says it verified.
- **line 319** · _security_ — getRoomLoginWhatsAppConfirmed hands the room tokens to whoever polls with the login code, and the code is not tied to the browser that started the login — so an attacker who gets a victim to send his prefilled WhatsApp message receives the victim's room tokens.
  <br>Failure: Attacker opens LOGIN on top-rated.team, presses WhatsApp, and receives an offer containing `code` = e.g. RSTU2345WXYZ plus a wa.me URL and a QR encoding "Room-login RSTU2345WXYZ" to the house number. He sends that QR/link to a customer ("scan this to reach us on WhatsApp"). The customer scans it and sends the message from the chat that IS bound to their room. proveRoomLoginWhatsApp resolves that chat's bindings and stores the customer's room tokens under the attacker's code. The attacker's browser is still polling GET /api/room-login/whatsapp/confirmed?code=RSTU2345WXYZ — an endpoint that reads only req.query.code, with no cookie, no session and no per-browser secret — and receives `{confirmed:true, rooms:[{token:"<customer room token>"}]}`, which the client at RoomMenu.tsx:183 turns into /w/<customer token>. A room token is a bearer credential, so the attacker now reads and writes in the customer's room.
- **line 323** · _authorisation_ — The WhatsApp login result is keyed only by the login code, so whoever holds the code collects the rooms — and the code is the text the code-holder asks a third party to send.
  <br>Failure: Nothing ties the browser that called GET /api/room-login/whatsapp to the WhatsApp chat that later proves. startRoomLoginWhatsApp returns `code` to the caller in the offer (server/room-login.ts:314, exposed as RoomLoginWhatsAppOffer.code in shared/api.ts:599); proveRoomLoginWhatsApp stores the rooms of WHATEVER chat sent that code (server/room-login.ts:339-341) without checking the chat against anything; getRoomLoginWhatsAppConfirmed hands those rooms to anyone who presents the code, with no cookie, no session and no second secret. Attack: attacker calls GET /api/room-login/whatsapp, receives {url: "https://wa.me/<house digits>?text=Room-login K7M2P...", qrSvg, code: "K7M2P..."}, and sends the victim that wa.me link or that QR ("scan to verify your room"). The victim taps it, their own WhatsApp sends "Room-login K7M2P..." to the house number from the victim's chat, the webhook resolves listBindingsByPerson("whatsapp", hash(victim chatId)) and writes the victim's room tokens under K7M2P.... The attacker, who never stopped polling GET /api/room-login/whatsapp/confirmed?code=K7M2P..., receives {"confirmed":true,"rooms":[{"token":"<victim room token>"}]} and opens /w/<victim room token> with full read and write. The room token is a bearer credential, so this is a complete handover of someone else's room to a stranger who only had to get one link tapped. Contrast identity.ts's bind flow, which deliberately does not return its nonce to the browser and polls by the workspace token the browser already holds.
- **line 324** · _replay_ — A confirmed WhatsApp login result is never consumed — the same code returns the room tokens on every poll for a full fifteen minutes.
  <br>Failure: getRoomLoginWhatsAppConfirmed reads whatsappResults with .get and returns rooms without deleting the entry; the only removal is the PENDING_MS sweep at line 112-114. The code is not a private value: by design it has been typed into a WhatsApp conversation, where it stays in both parties' chat history, in any screenshot of that chat, in the WhatsApp Web session of anyone with the house phone, and in the QR the browser rendered on screen. Concrete: a customer logs in over WhatsApp on a shared machine at 14:00; the login code sits in the house WhatsApp thread and in the browser's network log. At any point up to 14:15 anyone who can read that thread issues GET /api/room-login/whatsapp/confirmed?code=<code> and is handed the customer's room tokens — bearer credentials with no expiry of their own. A code that has already been redeemed once should be gone; nothing here removes it, and there is no test asserting a second poll fails.

### `server/routes.ts`

- **line 1390** · _authorisation_ — The house-only WhatsApp gate is decided from req.hostname, which behind `app.set("trust proxy", 1)` is the caller's own X-Forwarded-Host header, and HOUSE_HOSTS accepts "localhost".
  <br>Failure: server/index.ts:33 enables trust proxy, so Express reads req.hostname from X-Forwarded-Host whenever the immediate peer is trusted; the comment there reasons only about X-Forwarded-For spoofing and does not cover the host. HOUSE_HOSTS (shared/operator.ts:19-28) contains "localhost", "127.0.0.1" and "0.0.0.0". So on any fork, `curl -H 'X-Forwarded-Host: localhost' https://fork.example.com/api/room-login/whatsapp` makes isHouseHost(input.host) true at server/room-login.ts:143 and startRoomLoginWhatsApp proceeds — the only server-side check of the house rule for this route. On a fork that has Unipile configured at all, probeWaha then returns digits and the fork is handed a working WhatsApp login it was never meant to have; and because probeWaha falls back to publishedDigits() when the Unipile account body carries no phone number (server/waha.ts:123, parsed straight out of shared/roster.ts WHATSAPP_URL), the reply to that spoofed request can be a wa.me link to the owner's personal mobile. The same header flips GET /api/room-login (server/routes.ts:1343), so the fork's panel draws the third button too. The file header at server/room-login.ts:9-11 claims this decision "lives in HOUSE_HOSTS / isHouseHost, which a fork cannot reach around"; it is reached around with one request header.
- **line 1390** · _fork-safety_ — The server takes its house/fork decision from `req.hostname`, which is the caller-supplied Host / X-Forwarded-Host header under `app.set("trust proxy", 1)`, so the house-only rule can be flipped from outside the deployment.
  <br>Failure: On any fork with Unipile configured, `curl -H 'X-Forwarded-Host: top-rated.team' https://partner-fork.example/api/room-login/whatsapp` (or plain `-H 'Host: top-rated.team'` where no rewriting proxy sits in front) makes `req.hostname` report a house host, `roomLoginAvailability` returns `whatsapp:{available:true}`, and the route replies with the wa.me offer — the same offer that carries the owner's number whenever probeWaha's `publishedDigits()` fallback is hit. The same header on GET /api/room-login (line 1343) makes the fork's own login panel draw the WhatsApp control. The canonical-host middleware at routes.ts:513 cannot help: it returns early for every `/api/` path. The booking gate deliberately avoids this by preferring PUBLIC_BASE_URL over a caller-supplied host (server/booking/hold.ts:66-67); the new login gate does not.
- **line 1450** · _security_ — Defect 1 is not fixed, only renamed: POST /api/workspaces/:token/room-address binds ANY address to any room whose token the caller holds, and room tokens are handed to anonymous callers on request, so the deployment can still be made to mail a stranger a working link into an attacker's room.
  <br>Failure: 1) `POST /api/workspaces` with body `{}` — no auth, only createWorkspaceLimit — returns `workspace.token` (shared/api.ts:11), an attacker-controlled room. 2) `POST /api/workspaces/<attacker-token>/room-address` with `{"email":"victim@company.test"}`; requireWorkspace (routes.ts:200) only checks that the URL token resolves — it is the attacker's own room — and bindRoomAddressForToken (server/room-access.ts:392) does no confirmation of the address, so `room_address_bindings` now says victim@company.test owns the attacker's room. 3) `POST /api/room-access` with `{"email":"victim@company.test"}` — sendRoomAccessLink finds that binding via roomsForEmail and mails the victim, from the verified LEAD_EMAIL_FROM sender on top-rated.team, subject "A link to your room", a link that redirects to /w/<attacker-token>. The victim lands in a room the attacker also holds the bearer token for and reads everything typed there. The sealed defect said "any anonymous caller sets it on their own room… mail a stranger a working link into a room the attacker controls, from our domain and our sender" — that sentence is still true word for word; only the field name changed from workspaces.visitorEmail to a row in room_address_bindings. Worse, the PK is (emailHash, workspaceId), so the attacker's room is ADDED to a genuine customer's bindings and the mail lists it beside their real rooms as "Each of these links opens one room once". The comment at server/room-access.ts:387-391 ("this is not a way to bind a stranger's address to a room you do not have") is technically true and answers the wrong question: the attack binds a stranger's address to a room you DO have.

---

## 2. LinkedIn hide sweep — 75 surfaces confirmed reachable

204 surfaces claimed, 117 verified before the limit, 75 confirmed. The rest are
unverified and listed after. `hidden: true` is already set on the
`linkedin-automation` door (shared/doors.ts) and on the `linkedin-automation`
and `linkedin-dev` agents (shared/roster.ts); the flag alone does NOT cover
these.

### `/Users/dan/Documents/ChatGPT Ads/client/index.html`

- **line 39** · subject: `door linkedin-automation` — The site-wide meta description names a non-Ads LinkedIn service in the clear: '...paid advertising and the measurement under it, Google Ad Grants, inbound LinkedIn on the official API, and custom AI around all of it.' 'Inbound LinkedIn on the official API' is the linkedin-automation offer. This head is served for EVERY route (the comment at lines 8-11 says so), it is what a non-JavaScript crawler 
  <br>Fix: Edit the sentence in place — the surrounding clauses are already generic enough to drop the LinkedIn clause without rewriting the tag. No build step touches this file.
- **line 59** · subject: `door linkedin-automation` — og:description repeats the same string verbatim, including 'inbound LinkedIn on the official API'. This is the copy that actually travels: it is what Slack, LinkedIn's own post-preview scraper, iMessage and every other unfurler renders when the domain is pasted, and those scrapers do not run JavaScript, so no per-route override reaches them (lines 31-34 state that per-route meta injection is unimp
  <br>Fix: Edit alongside line 39 and line 80 — the three must stay identical or the card and the snippet disagree.
- **line 80** · subject: `door linkedin-automation` — twitter:description, the third verbatim copy of the same sentence with 'inbound LinkedIn on the official API' in it. Paired with twitter:card=summary_large_image at line 76, so it renders as the body text of the wide share card.
  <br>Fix: Edit with lines 39 and 59 in the same pass.

### `/Users/dan/Documents/ChatGPT Ads/client/public/.well-known/ai-plugin.json`

- **line 6** · subject: `door linkedin-automation and door linkedin-growth (via the connector)` — description_for_model instructs any arriving model: 'Use list_services to explain the offers and who invoices each one.' Line 10 points api.url at https://ai.top-rated.team/api/connector/openapi.json with auth type none (line 7). This static manifest is the advertised front door to the unfiltered catalogue above — it is the reason a machine finds the hidden doors at all. robots.txt Disallow: /api/
  <br>Fix: No edit needed here if handlers.ts:108 is filtered — the manifest names no service itself. Report it as the discovery path so the connector fix is not treated as optional.

### `/Users/dan/Documents/ChatGPT Ads/client/public/.well-known/mcp.json`

- **line 8** · subject: `door linkedin-automation and door linkedin-growth (via MCP)` — The second machine entry point: a streamable-http MCP remote at https://ai.top-rated.team/api/connector/mcp. server/connector/mcp.ts exposes the same listServices handler as an MCP tool, so a Claude or ChatGPT connector enumerates the hidden doors exactly as the REST route does.
  <br>Fix: Same single fix at server/connector/handlers.ts:108 — both mounts call one implementation, which the file header at server/connector/index.ts:1-8 states explicitly.

### `/Users/dan/Documents/ChatGPT Ads/client/public/llms.txt`

- **line 39** · subject: `door linkedin-automation` — The generated index still lists '[LinkedIn automation — with a written legal assessment](https://top-rated.team/services/linkedin-automation)' with its full blurb and tier. build-llms.ts:92 does honour the flag, but this file is committed to the repo and is only rewritten by `npm run prebuild` — so setting the flag without a rebuild changes nothing that a crawler fetches.
  <br>Fix: Covered by the flag, but only after regeneration: run `npx tsx scripts/build-llms.ts` (or `npm run build`, whose prebuild does it) and commit the two files.
- **line 39** · subject: `door:linkedin-automation` — The index line for the linkedin-automation door, with its headline, blurb and URL, in the file every arriving AI agent reads first.
  <br>Fix: Same: set hidden and re-run scripts/build-llms.ts.

### `/Users/dan/Documents/ChatGPT Ads/client/public/robots.txt`

- **line 1** · subject: `none — verified` — VERIFIED CLEAN — reported so the sweep is closed rather than because it needs an edit. Eleven lines: Allow: /, Disallow: /w/, Disallow: /api/, and the Sitemap line. It names no LinkedIn door, and it must not: a Disallow line publishes the address just as loudly as a <loc> does, which is the argument sitemap.xml already makes at lines 24-25. Note that Disallow: /api/ is advisory only and does not s
  <br>Fix: No action. Do not add a Disallow for the hidden doors; that would defeat the purpose.

### `/Users/dan/Documents/ChatGPT Ads/client/public/sitemap.xml`

- **line 74** · subject: `door linkedin-automation` — <loc>https://top-rated.team/services/linkedin-automation</loc> — the hidden-to-be door's address handed straight to every crawler that fetches /sitemap.xml. The worst surface in the sweep: the file is HAND-WRITTEN and nothing in scripts/ or package.json regenerates it (grep for a writer of sitemap.xml returns only prose references inside build-llms.ts). Its own header comment at lines 6-13 admits 
  <br>Fix: Delete the <url> block at lines 73-77 by hand. Nothing regenerates this file, so the flag can never reach it. The durable fix is the handoff the file's own comment names: generate sitemap.xml from PUBLIC_DOORS in scripts/build-llms.ts, which already computes exactly that list at line 92 and already 
- **line 74** · subject: `door linkedin-automation` — <loc>https://top-rated.team/services/linkedin-automation</loc> is handed to every crawler. The file is static and hand-copied (its own comment at lines 6-13 admits the URLs were copied out of the PUBLIC_DOORS rule and says it should be generated), so no code flag can ever reach it. It correctly omits linkedin-growth only because a person remembered to leave it out.
  <br>Fix: Delete the <url> block at lines 73-77 by hand, and add linkedin-automation to the 'absent on purpose' note at lines 18-28. There is no generator for this file; the flag cannot cover it until one exists.

### `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/CaseFilter.tsx`

- **line 25** · subject: `door linkedin-automation (latent)` — `const DOORS_WITH_CASES = DOORS.filter((door) => casesForDoor(door.id).length > 0);` — no hidden filter. It builds the Service dropdown on the public /case-studies page (which is in sitemap.xml twice, lines 39 and 84). Latent rather than live today: shared/cases.ts:13-14 records that linkedin-ads, linkedin-automation, ai-builds, white-label and the partner row have no case, so no LinkedIn option r
  <br>Fix: Add `!door.hidden &&` at line 25. One token, and it forecloses a regeneration-triggered leak that nothing else would catch.

### `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/Footer.tsx`

- **line 56** · subject: `door linkedin-growth` — useSomebodyElsesDoor() matches /services/:slug and looks the slug up in DOOR_BY_SLUG with NO hidden check. Because NotFound renders <Footer /> (not-found.tsx:70), visiting /services/linkedin-growth today shows a 404 body with a footer block above it reading: 'The work on this page is Maksymenko LinkedIn Growth's. Maksymenko LinkedIn Growth invoices you directly... Maksymenko LinkedIn Growth has no
  <br>Fix: Add the hidden check at line 57: `if (!door || door.hidden) return null;`

### `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/GatedOffers.tsx`

- **line 64** · subject: `door:linkedin-growth, door:linkedin-automation` — LISTED_DOORS = DOORS.filter(!hidden) — the list /services (doors.tsx:91) and the home page index (DoorIndex.tsx:61) both render. Honours the flag.
  <br>Fix: Already covered.
- **line 82** · subject: `door linkedin-growth` — `export const PARTNER_DOORS = DOORS.filter((door) => !isPublicDoor(door));` — the one door-list export in this file that omits the !door.hidden test its two neighbours at lines 64 and 79 both carry. linkedin-growth is tier grey, so it is the sole member of PARTNER_DOORS: a hidden door sitting in an exported list. It currently has no consumer (grep across client/, server/ and scripts/ finds only th
  <br>Fix: Add `!door.hidden &&` to match lines 64 and 79, or delete the unused export. Part of the 'verify linkedin-growth is complete' answer: the door is off every rendered list, but not off every list.
- **line 82** · subject: `door:linkedin-growth` — PARTNER_DOORS = DOORS.filter(!isPublicDoor) — no hidden filter, unlike LISTED_DOORS (line 64) and PUBLIC_DOORS (line 79) directly above it. It resolves to exactly [linkedin-growth], the hidden door. Nothing renders it today (GATED_DOORS is []), so it is a loaded gun rather than a live leak.
  <br>Fix: Add `!door.hidden &&` for consistency with its two neighbours, before someone renders it.
- **line 131** · subject: `LinkedIn growth/automation products (the same two the hidden linkedin-growth door sells)` — GATED_PRODUCTS hard-codes two LinkedIn engagement products with live outbound links, rendered on the public /services page: 'Top-Voice' / top-voice.ai — 'Engagement on LinkedIn from profiles you already own — commenting and replying where the people you sell to are reading' (lines 132-139), and 'Warmlike' / warmlike.com — 'warming up the people on it before anyone sends them a pitch' (lines 140-14
  <br>Fix: Empty the array the way GATED_DOORS at line 89 was already emptied — the guard at line 230 then returns null and the whole section disappears, exactly as its own comment anticipates ('Keeps an empty section off the page if every row is ever made public').
- **line 137** · subject: `LinkedIn service, outside the door table` — The Top-Voice product row on /services, behind the email step: "Engagement on LinkedIn from profiles you already own — commenting and replying where the people you sell to are reading..." with a live link to top-voice.ai. This is a LinkedIn engagement service on display, and it is a hand-written list that no door flag touches.
  <br>Fix: Remove the row from GATED_PRODUCTS (or give GatedProduct its own hidden flag). Note the section auto-hides when GATED_PRODUCTS and GATED_DOORS are both empty (line 230), so removing both LinkedIn rows removes the section.
- **line 294** · subject: `LinkedIn service, outside the door table` — The gate's own copy, shown BEFORE the email step to any visitor on /services: "What they have in common today is LinkedIn — activity carried out on real profiles, ours or yours, rather than through an ad account." It advertises unnamed LinkedIn activity services on the open page.
  <br>Fix: Rewrite or remove with the rows it describes.
- **line 449** · subject: `door:linkedin-automation` — "They act on real LinkedIn accounts, and LinkedIn has rules about that which it enforces... that is the LinkedIn automation door above — a lawyer writes the assessment first." A direct pointer, in prose, to the door that is supposed to disappear.
  <br>Fix: Remove the sentence; it will also become false once the door is hidden.

### `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/doors/AiBuildsBody.tsx`

- **line 65** · subject: `LinkedIn automation product Top-Voice` — The SHOWN['top-voice'] entry, rendered on the PUBLIC, live, white-tier /services/ai-builds page — AiBuildsBody maps over the whole BUILDS array (line 159) and prints anything with a SHOWN key. The visitor reads 'A LinkedIn visibility platform built around reactions rather than outreach... it builds a feed of those people's posts and reacts to them from a profile or company page' plus six bullets i
  <br>Fix: Delete the 'top-voice' key from SHOWN — the render loop's `if (!shown) return null;` (line 161) already drops any build with no entry, so no other edit is needed. Note the page's own copy says 'The six below', so the count sentence at line 154 needs adjusting too.
- **line 65** · subject: `LinkedIn service, outside the door table` — The SHOWN copy for "top-voice" on the LIVE, public /services/ai-builds door page: "A LinkedIn visibility platform built around reactions rather than outreach... reacts to them from a profile or company page... a reaction engine with human-like pacing... a pool of dedicated in-house LinkedIn accounts... LinkedIn DM entry points." This is the most detailed LinkedIn automation description on the whol
  <br>Fix: Delete the "top-voice" key from SHOWN — the renderer at line 162 already does `if (!shown) return null`, so removing the key drops the row with no other change.
- **line 66** · subject: `LinkedIn service (Top Voice reaction engine) on the ai-builds door` — The live, public, sitemap-listed /services/ai-builds page prints 'Top Voice' as build 03, linked to top-voice.ai, described as 'A LinkedIn visibility platform built around reactions rather than outreach... it builds a feed of those people's posts and reacts to them from a profile or company page', with bullets naming 'A LinkedIn Booster: boolean searches... and a reaction engine with human-like pa
  <br>Fix: Delete the 'top-voice' key from the SHOWN map (lines 65-75). The map is already the render gate — a build absent from SHOWN returns null at line 161 — so this is a one-key removal with no other change.
- **line 76** · subject: `LinkedIn automation product Warmlike` — The SHOWN['warmlike'] entry on the same public /services/ai-builds page: 'A LinkedIn visibility service around a second, managed company page rather than the client's own account... That page engages the posts of the people the client sells to, and reshares the client's own posts', with bullets naming 'the managed company page — a pre-lander — run as the service, not as access to the client's Link
  <br>Fix: Delete the 'warmlike' key from SHOWN in the same edit as top-voice.
- **line 76** · subject: `LinkedIn service, outside the door table` — The SHOWN copy for "warmlike" on the same public page: "A LinkedIn visibility service around a second, managed company page... engages the posts of the people the client sells to, and reshares the client's own posts."
  <br>Fix: Delete the "warmlike" key from SHOWN, same mechanism.
- **line 77** · subject: `LinkedIn service (WarmLike) on the ai-builds door` — Same page prints 'WarmLike', linked to warmlike.com: 'A LinkedIn visibility service around a second, managed company page... That page engages the posts of the people the client sells to, and reshares the client's own posts.' Another third-party LinkedIn engagement service on a public page.
  <br>Fix: Delete the 'warmlike' key from the SHOWN map (lines 76-83).
- **line 160** · subject: `LinkedIn service, outside the door table` — BUILDS.map renders each build's NAME and its host as a link — "Top Voice / top-voice.ai" and "WarmLike / warmlike.com" — from shared/builds.ts:97-99 and :130-132, whose oneLine fields read "Organic LinkedIn growth engine that turns targeted reactions into inbound traffic" and "A fully managed LinkedIn pre-lander company page...".
  <br>Fix: Removing the SHOWN keys is enough for the page; if the names must go from the generated data too, drop the rows in data/builds and re-run scripts/build-builds.ts.

### `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/home/HouseAsk.tsx`

- **line 46** · subject: `agent:linkedin-automation, door:linkedin-automation` — The home page panel. It renders route.reason (line 353) — the sentence naming the agent — the choice list (line 359), and then streams that agent's answer via /api/ask. EXAMPLES at line 46 comes from ANSWERING_DOORS and today happens to take the first three, so linkedin-automation is not among them; that is luck of ordering, not a filter.
  <br>Fix: Fixed upstream in route-question.ts and doorText.ts.

### `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/home/doorText.ts`

- **line 36** · subject: `door:linkedin-automation` — ANSWERING_DOORS = DOORS.filter(status === "live" && firstAgentId) — no hidden filter. It is the home page's list of "doors whose panel answers today", and the linkedin-automation door satisfies both conditions.
  <br>Fix: Add `&& !door.hidden`.

### `/Users/dan/Documents/ChatGPT Ads/client/src/components/workspace/Composer.tsx`

- **line 46** · subject: `agent linkedin-automation and agent linkedin-dev` — `AGENTS.map(...)` builds the @mention autocomplete. In any room, typing '@lin' offers 'LinkedIn Automation Agent' and 'LinkedIn Dev Agent' by name and handle, and mentioning one makes it answer.
  <br>Fix: No mechanism exists. Needs a hidden flag on AgentDef filtered here.
- **line 46** · subject: `agent:linkedin-automation, agent:linkedin-dev` — The @mention autocomplete. mentionOptions() maps every row of AGENTS into the menu; the menu (line 319) prints @handle and the agent's title. Typing "@link" in any room offers @linkedin-ads, @linkedin-automation and @linkedin-dev side by side.
  <br>Fix: Build mentionOptions from VISIBLE_AGENTS. Hiding it here does NOT stop the mention working — see server/routes.ts:206.

### `/Users/dan/Documents/ChatGPT Ads/client/src/components/workspace/MemberRail.tsx`

- **line 271** · subject: `agent:linkedin-automation, agent:linkedin-dev` — The member rail renders any agent member row present in the room: MemberGlyph (line 271, agent.mark/initials), roleFor (line 303, agent.title) and hoverForKey (line 375, agent.title on hover). An existing room already seeded with or joined by the LinkedIn agents keeps showing them regardless of what the roster lists.
  <br>Fix: Filter agent members whose roster row is hidden out of the rail, so already-created rooms stop displaying them. Do not remove the row from AGENT_BY_ID — that would break the name lookup for historical messages.

### `/Users/dan/Documents/ChatGPT Ads/client/src/components/workspace/MessageList.tsx`

- **line 47** · subject: `agent:linkedin-automation` — starterQuestions() prints four one-click starter questions from the channel's agent, or from the door's firstAgentId. In a linkedin-automation channel or room the visitor is offered "What can be automated on LinkedIn without putting the account at risk?" and three more.
  <br>Fix: Return [] when the resolved agent is hidden.

### `/Users/dan/Documents/ChatGPT Ads/client/src/components/workspace/TaskPanel.tsx`

- **line 116** · subject: `agent:linkedin-automation, agent:linkedin-dev` — A task's assignee chip resolves `agent:<id>` through AGENT_BY_ID and prints the agent's name and initials. Any task assigned to a LinkedIn agent names it in the task panel.
  <br>Fix: Same as MessageItem — display of existing data; gated by stopping the agent being reachable in the first place.

### `/Users/dan/Documents/ChatGPT Ads/client/src/components/workspace/WorkspaceSidebar.tsx`

- **line 139** · subject: `agent:linkedin-automation, agent:linkedin-dev` — The room's Agents rail. `here`/`elsewhere` are built by filtering the WHOLE AGENTS array; six rows print by default (line 217) and the "N more" button at line 239 expands to all fourteen. A visitor in any room reads "LinkedIn Automation Agent" and "LinkedIn Dev Agent" as clickable rows, and clicking one opens a live channel with it.
  <br>Fix: Filter both `here` and `elsewhere` through VISIBLE_AGENTS. Note `here` must be filtered too, or a room already seeded with the agent still shows it.
- **line 140** · subject: `agent linkedin-automation and agent linkedin-dev` — `elsewhere: AGENTS.filter((agent) => !inRoom.has(...))` — the rest of the roster, listed in the room sidebar. Six show by default and the remainder appear behind one 'show all' line (line 156-158). linkedin-automation is index 9 and linkedin-dev index 14 of 15, so both are one click away for every visitor in every room.
  <br>Fix: No mechanism exists. Needs a hidden flag on AgentDef filtered here.

### `/Users/dan/Documents/ChatGPT Ads/client/src/pages/landing.tsx`

- **line 39** · subject: `door linkedin-automation` — PAGE_DESCRIPTION is a fourth hand-typed copy of the same sentence, 'inbound LinkedIn on the official API' included, written into meta[name=description] at runtime (lines 48-57) for the home page. So the string survives even if the static head is corrected: a JavaScript-capable crawler on / gets this one instead. The comment at line 37 notes the description deliberately avoids listing rows 'that wo
  <br>Fix: Fix in the same pass as the three index.html tags; four hand-written copies of one sentence and no single source for it.

### `/Users/dan/Documents/ChatGPT Ads/client/src/pages/setup.tsx`

- **line 381** · subject: `door linkedin-automation` — Renders `{door.headline}` for every row from GET /api/operator. /setup is a public wouter route (App.tsx:63) reachable on the house host, so a visitor typing /setup reads 'LinkedIn automation — with a written legal assessment' as a service offered here, with named / white-label radio buttons beside it.
  <br>Fix: Fixed by filtering in shared/operator.ts:90; no change needed at this line.
- **line 381** · subject: `door:linkedin-automation` — /setup and /partner are public routes (App.tsx:63-64). The page renders one row per operator door printing `door.headline` — so "LinkedIn automation — with a written legal assessment" is on a public page with a checkbox and radio buttons beside it.
  <br>Fix: Fixed by filtering operatorDoors; the page just maps what the API returned.

### `/Users/dan/Documents/ChatGPT Ads/server/ai/agentRuntime.ts`

- **line 68** · subject: `agent:linkedin-automation, agent:linkedin-dev` — streamAgentAnswer resolves `AGENT_BY_ID[opts.agentId] ?? AGENT_BY_ID[DEFAULT_AGENT_ID]` with no visibility check, then retrieves from that agent's kbNamespace (data/kb/kb.linkedin-automation.json is on disk and loaded) and streams an answer whose citations link to LinkedIn's own developer pages.
  <br>Fix: Last-line defence: refuse a hidden agent here and yield the same message an unconfigured agent yields, so no caller can reach the corpus even if a new entry point is added later.

### `/Users/dan/Documents/ChatGPT Ads/server/ai/route-question.ts`

- **line 246** · subject: `door linkedin-automation` — scoreDoors() builds its document set from raw `DOORS`. Every hidden door's id, slug, headline, blurb, agentLine and four starters enter the scorer. A visitor typing 'can we automate LinkedIn invitations?' into the home-page panel scores against the linkedin-automation row and wins it.
  <br>Fix: Filter at the top of this module — a single `const ROUTABLE = DOORS.filter((d) => !d.hidden)` used everywhere DOORS is read in this file (lines 246, 469, 505).
- **line 435** · subject: `door linkedin-automation and agent linkedin-automation` — reasonForDoor() prints, verbatim to the visitor on the home page: 'This looks like a question about LinkedIn automation, so the LinkedIn Automation Agent is answering.' HouseAsk.tsx:353 renders that sentence, and the routed agentId is then posted to /api/ask so that agent actually answers with citations from LinkedIn's developer documentation. This names both the hidden door and a hidden agent in 
  <br>Fix: Nothing to change here once scoreDoors and fillChoices stop returning hidden rows — this function only formats what they picked.
- **line 441** · subject: `agent:linkedin-automation, door:linkedin-automation` — reasonForDoor() prints the AGENT'S NAME to the visitor on the home page: "This looks like a question about ... so the LinkedIn Automation Agent is answering." (and line 437 for a picked door). The whole module scores against DOORS with no hidden filter.
  <br>Fix: Filter DOORS to non-hidden rows at the top of this module — one constant used by scoreDoors, fillChoices, uniqueComingDoor and pickedLiveDoor.
- **line 456** · subject: `door:linkedin-automation` — fillChoices() offers two or three live doors as clickable choices when routing is uncertain, labelled with the door's headline. The linkedin-automation door is live with an agent, so "LinkedIn automation — with a written legal assessment" can be offered on the home page.
  <br>Fix: Covered by the single DOORS filter above.
- **line 505** · subject: `door linkedin-growth` — uniqueComingDoor() scans raw DOORS for a token carried by exactly one non-live door. linkedin-growth matches on tokens like 'maksymenko' or 'growth'. The visitor gets CONTACT_COMING, which does not name the door — so nothing leaks today, but only because that constant happens to be anonymous, not because the row is hidden.
  <br>Fix: Same single filter as line 246; the protection here is currently accidental and should not be relied on.
- **line 513** · subject: `door linkedin-automation and agent linkedin-automation` — pickedLiveDoor() honours any doorId the client posts as long as DOOR_BY_ID has it and status is 'live'. POST /api/route-question with {question, doorId:'linkedin-automation'} returns kind:'door' with agentId 'linkedin-automation' and the reason line naming the agent, straight past every list filter.
  <br>Fix: Add `|| door.hidden` to the rejection at line 514-515, so a hidden id is ignored the way a coming one already is.

### `/Users/dan/Documents/ChatGPT Ads/server/connector/handlers.ts`

- **line 108** · subject: `door linkedin-automation and door linkedin-growth` — listServices() is `return { services: DOORS.map(publicService) };` — every row in shared/doors.ts, with no hidden filter and no tier filter. publicService (line 82) emits headline, blurb, path, agentLine, all four starters, contract and firstAgentId. So GET /api/connector/services returns the full 'LinkedIn automation — with a written legal assessment' row AND the already-hidden linkedin-growth ro
  <br>Fix: Filter at line 108: `DOORS.filter((door) => !door.hidden).map(publicService)`. This is the single highest-value uncovered consumer — one line closes both doors on both the REST and MCP mounts, since server/connector/mcp.ts calls the same handler.
- **line 108** · subject: `door:linkedin-automation, door:linkedin-growth` — listServices() returns `DOORS.map(publicService)` with NO hidden filter. GET /api/connector/services (unauthenticated, CORS *) and the MCP tool list_services therefore publish the linkedin-automation door in full — headline, blurb, agentLine, four starters, firstAgentId "linkedin-automation" — AND the supposedly hidden linkedin-growth door. This is the single biggest hole in the existing door mech
  <br>Fix: `DOORS.filter(d => !d.hidden).map(publicService)`. Also drop `firstAgentId` when it names a hidden agent, or a machine will pass it to ask_question.
- **line 145** · subject: `agent:linkedin-automation, agent:linkedin-dev` — The connector's askQuestion() — the body behind both the Custom GPT Action POST /api/connector/ask and the MCP tool ask_question. Same unguarded AGENT_BY_ID validation, then the same streamAgentAnswer. CORS is wide open (connector/index.ts:32) and there is no auth.
  <br>Fix: Reject a hidden agentId with the same `Unknown agent` 400.
- **line 212** · subject: `door:linkedin-automation, door:linkedin-growth` — createRoom() validates `doorId` against DOOR_BY_ID with no hidden filter, so an MCP/GPT caller can open a room stamped with linkedin-automation (or linkedin-growth) and trigger the seeding above.
  <br>Fix: Reject a hidden doorId as unknown.

### `/Users/dan/Documents/ChatGPT Ads/server/connector/mcp.ts`

- **line 36** · subject: `door:linkedin-automation, door:linkedin-growth, agent:linkedin-automation` — MCP tool list_services, described as "List every offer Top-Rated Team publishes... and which agent answers first". It calls the unfiltered listServices() above, so an MCP client (Claude, ChatGPT) is told about the LinkedIn automation service and its agent.
  <br>Fix: Fixed by filtering listServices() in handlers.ts:108 — the tool is a wrapper.
- **line 243** · subject: `door linkedin-automation and door linkedin-growth` — The MCP tool `list_services` returns the same unfiltered payload to any Claude client that connects to /api/connector/mcp. Its OpenAPI description (openapi.ts:30) even instructs the model to 'Quote the fields as returned' and 'Use this to explain the company'.
  <br>Fix: Fixed by filtering in handlers.ts:108; no change needed at this line.

### `/Users/dan/Documents/ChatGPT Ads/server/connector/openapi.ts`

- **line 12** · subject: `agent linkedin-automation and agent linkedin-dev` — `const AGENT_IDS = Object.keys(AGENT_BY_ID)` becomes an `enum` on the ask_question agentId parameter (line 103) in the OpenAPI document served publicly at GET /api/connector/openapi.json. It publishes 'linkedin-automation' and 'linkedin-dev' as callable agent ids to any machine that fetches the schema.
  <br>Fix: No mechanism exists. Add a hidden/unlisted flag to AgentDef and filter this list, or hard-exclude the two ids here.
- **line 12** · subject: `agent:linkedin-automation, agent:linkedin-dev` — AGENT_IDS = Object.keys(AGENT_BY_ID) — every agent id — is published verbatim as the `enum` of the agentId parameter at line 103 of GET /api/connector/openapi.json. A machine reading the OpenAPI document is handed "linkedin-automation" and "linkedin-dev" as a menu.
  <br>Fix: Build AGENT_IDS from VISIBLE_AGENTS.
- **line 103** · subject: `agent linkedin-automation and agent linkedin-dev` — `enum: AGENT_IDS` inside the agentId property of the /ask request body. AGENT_IDS is `Object.keys(AGENT_BY_ID)` (line 12) — the entire roster. The public OpenAPI 3.1 document served at /api/connector/openapi.json therefore lists the literal strings 'linkedin-automation' and 'linkedin-dev' as selectable agents, and the enum doubles as a working call: POST /api/connector/ask with agentId 'linkedin-d
  <br>Fix: There is no flag to honour — see shared/roster.ts:9. Add a hidden?: boolean to AgentDef, set it on both agents, then filter AGENT_IDS at openapi.ts:12 and reject a hidden agentId in the ask handler. Filtering the enum alone still leaves the agent callable.

### `/Users/dan/Documents/ChatGPT Ads/server/routes.ts`

- **line 206** · subject: `agent:linkedin-automation, agent:linkedin-dev` — AGENT_KEYS is built from every row of AGENTS, mapping both handle and id. resolveAgent() (line 337) then matches any @mention in any message body in any room. A visitor who types "@linkedin-automation" — or who has an old room where the sidebar once offered it — still gets a full grounded answer even if every client list is filtered. THIS is the surface that makes list-filtering alone insufficient
  <br>Fix: Skip hidden agents when populating AGENT_KEYS, so the mention resolves to null and nobody answers.
- **line 336** · subject: `agent:linkedin-automation, agent:linkedin-dev` — ensureAgentSurface() adds the agent as a room member (displayName = agent.name, role = agent.title, presence online) and creates a channel named after its handle. It does no visibility check, and it dereferences AGENT_BY_ID[agentId] without a null guard.
  <br>Fix: Callers must be gated upstream; if a resolver is added, keep AGENT_BY_ID complete here or add a guard.
- **line 616** · subject: `agent:linkedin-automation, agent:linkedin-dev` — POST /api/workspaces accepts `agentId` and validates it against AGENT_BY_ID. A room can be opened seeded on the LinkedIn Automation Agent, which then gets a member row, a channel named after its handle, and answers the first message.
  <br>Fix: Same rejection as /api/ask.
- **line 797** · subject: `agent:linkedin-automation, agent:linkedin-dev` — POST /api/workspaces/:token/channels accepts an arbitrary `counterpartKey` (schema.ts:182 allows any string up to 80 chars). A client can create a channel with counterpartKey "agent:linkedin-automation" without the agent being on any list; messages in it then route through resolveAgent.
  <br>Fix: Reject a counterpartKey naming a hidden agent, or rely on the AGENT_KEYS gate at line 206 so the channel exists but nothing answers in it.
- **line 990** · subject: `agent:linkedin-automation, agent:linkedin-dev` — POST /api/ask — unauthenticated, rate-limited only. `agentId` comes straight off the request body and is accepted if AGENT_BY_ID has it. Anyone can POST {agentId:"linkedin-automation", question:"..."} and receive a streamed answer citing LinkedIn's developer documentation. This is the highest-exposure reachability hole: no room, no token, no door page needed.
  <br>Fix: Reject a hidden agentId here exactly as an unknown one is rejected — `Unknown agent: <id>`, so the 400 says nothing about a row existing.
- **line 1000** · subject: `agent linkedin-automation and agent linkedin-dev` — POST /api/ask takes any agentId present in AGENT_BY_ID — no gate beyond existence. A visitor can invoke the LinkedIn Automation Agent or the LinkedIn Dev Agent directly from the public panel endpoint, with no room and no token.
  <br>Fix: No mechanism exists. Reject agents carrying a new hidden flag at the `if (!agent)` check on line 1002.

### `/Users/dan/Documents/ChatGPT Ads/server/seats.ts`

- **line 268** · subject: `agent:linkedin-automation, agent:linkedin-dev` — uniqueHandle() checks a proposed outside-agent handle against every AGENTS handle and id. Low exposure — it only reserves the names — but it means an admitted third-party agent cannot be called "linkedin-dev", which silently confirms the row exists.
  <br>Fix: Leave as is deliberately (reserving the name is harmless), or note it as an accepted leak.

### `/Users/dan/Documents/ChatGPT Ads/server/storage.ts`

- **line 206** · subject: `agent:linkedin-automation` — createWorkspace seeds agent member rows from the playbook's channel list — displayName = agent.name, role = agent.title, presence "online". A room stamped with the linkedin-automation door is born with a LinkedIn Automation Agent member.
  <br>Fix: Skip a seed whose agent row is hidden, so no new room is born with one.

### `/Users/dan/Documents/ChatGPT Ads/shared/catalogue.ts`

- **line 83** · subject: `door linkedin-automation and door linkedin-growth` — resolveCatalogue() maps over every row and never reads `hidden`. With no operator it returns the DOORS array itself, so the reference deployment is unaffected — but any fork's catalogue (server/operator.ts:144 seededCatalogue) carries the hidden rows through with `offered: true` by default (line 99).
  <br>Fix: Filter hidden rows out before the map at line 97, or preserve the flag and have every catalogue consumer honour it. Note the identity assertion in server/catalogue.test.ts:58-72 requires the no-operator path keep returning the same array object.

### `/Users/dan/Documents/ChatGPT Ads/shared/doors.ts`

- **line 447** · subject: `agent:linkedin-automation, door:linkedin-automation` — The linkedin-automation door's agentLine: "The LinkedIn Automation Agent answers first, from LinkedIn's own developer documentation..." This string is what the door page, the connector's list_services and llms-full.txt all print, so it names the agent on three surfaces at once.
  <br>Fix: Hiding the door plus filtering listServices removes every consumer of it; the string itself can stay for the day the row comes back.

### `/Users/dan/Documents/ChatGPT Ads/shared/operator.ts`

- **line 90** · subject: `door:linkedin-automation` — operatorDoors() filters only by contract legal name, not by hidden. GET /api/operator returns `doors: operatorDoors(DOORS)` (via publicOperator, line 344) on every request, including on the house host, and it is unauthenticated for GET.
  <br>Fix: Add `&& !door.hidden` to the filter in operatorDoors, and to defaultServices which derives from it.

### `/Users/dan/Documents/ChatGPT Ads/shared/playbook.ts`

- **line 177** · subject: `agent:linkedin-automation, door:linkedin-automation` — seedFor() pushes a channel `ask-${door.firstAgentId}` with counterpartKey `agent:${door.firstAgentId}` for any of our own doors. A room opened with source.door = "linkedin-automation" (reachable via the connector's create_room doorId, or a POST to /api/workspaces) gets an ask-linkedin-automation channel.
  <br>Fix: Skip the agent channel when the door or its agent is hidden; the project channel and the lawyer still seed.

### `/Users/dan/Documents/ChatGPT Ads/shared/pricing.ts`

- **line 132** · subject: `LinkedIn service (content autopilot) in the price ladder` — Price row 'ownKeys': buys = 'LinkedIn content autopilot, and the AI agents.' Rendered on the home page ladder and on /pricing (Ladder.tsx:166), and returned by GET /api/connector/prices and the MCP list_prices tool (handlers.ts:113). A LinkedIn posting service advertised as free, keyed to no door, so no door flag reaches it.
  <br>Fix: Reword the `buys` string to drop 'LinkedIn' (the row's own comment at 139-147 explains it is w_member_social publishing), or remove the row for the duration of the review.
- **line 184** · subject: `LinkedIn service (boosting) in the price ladder` — Price row 'support': buys = 'Content-generation support, or boosting support for top-voice.ai or warmlike.com.' — LinkedIn boosting, named with both domains, on the home page ladder, /pricing, and the connector's prices endpoint and MCP tool.
  <br>Fix: Reword the `buys` string to drop the two LinkedIn products, or remove the row for the duration of the review.
- **line 275** · subject: `LinkedIn service, spoken by every agent` — publishedPricesForPrompt() splices every price row's `buys` line into HOUSE_STYLE (roster.ts:110), which is prepended to EVERY agent's system prompt. So every agent on the roster — including the LinkedIn Ads Agent that stays visible — is instructed that "free — LinkedIn content autopilot, and the AI agents" is a published price it may repeat verbatim.
  <br>Fix: Fixed by rewording the row at line 132; nothing else needs to change.

### `/Users/dan/Documents/ChatGPT Ads/shared/roster.ts`

- **line 9** · subject: `agent linkedin-automation and agent linkedin-dev` — CONFIRMED AS ASKED: `export interface AgentDef` (lines 9-42) has id, handle, name, title, blurb, initials, mark, tone, useKb, kbNamespace, systemPrompt, starters — and no hidden, no listed, no status field of any kind. Grepping 'hidden' across shared/roster.ts returns only two unrelated prose hits (lines 509, 520). There is no agent-hiding mechanism in this codebase, so nothing whatsoever currentl
  <br>Fix: Add the field to AgentDef mirroring DoorDef.hidden at shared/doors.ts:174, set it on the two rows, then honour it at every AGENTS/AGENT_BY_ID consumer — the OpenAPI enum above being the machine-facing one.
- **line 526** · subject: `agent:linkedin-automation` — The linkedin-automation agent row itself: name "LinkedIn Automation Agent", title "Reads LinkedIn's own developer documentation", handle @linkedin-automation, kbNamespace linkedin-automation, four LinkedIn starter questions, and a system prompt about LinkedIn permissions.
  <br>Fix: Set hidden: true on the row once the flag exists.
- **line 752** · subject: `agent:linkedin-automation` — Prose inside the AI Lawyer Agent's system prompt — an agent seeded into EVERY room of ours: "That is LinkedIn's own API documentation and the LinkedIn Automation Agent reads it — @linkedin-automation in a room." The lawyer will name the hidden agent to a visitor and tell them how to summon it.
  <br>Fix: Delete the hand-off sentence; the surrounding paragraph still works as "that is not mine to answer".
- **line 909** · subject: `agent:linkedin-dev` — The linkedin-dev agent row: name "LinkedIn Dev Agent", title "Designs LinkedIn messaging and calendar integrations", handle @linkedin-dev, four starters that name LinkedIn messaging.
  <br>Fix: Set hidden: true on the row once the flag exists.
- **line 978** · subject: `agent:linkedin-automation, agent:linkedin-dev` — Prose inside the LinkedIn Dev Agent's own prompt: "Name @legal and @linkedin-automation for the permission half and for LinkedIn's own API documentation." Moot once linkedin-dev is hidden, but it is a second copy of the same pointer and must not survive a partial fix.
  <br>Fix: Removed with the row, or edited if the row is only delisted.
- **line 987** · subject: `mechanism: smallest place to honour a flag` — AGENT_BY_ID is the single choke point that makes an agent ANSWERABLE. Every server entry point (POST /api/ask, POST /api/workspaces, the room @mention resolver, the connector, the bridge, agentRuntime) validates an incoming agentId by looking it up here. It is also what MemberRail/MessageItem/TaskPanel use to RENDER an agent already seeded in a room, so it must not simply drop rows or ensureAgentS
  <br>Fix: Keep AGENT_BY_ID complete for rendering; add two exports next to it — `VISIBLE_AGENTS = AGENTS.filter(a => !a.hidden)` for every list, and `answerableAgent(id)` returning undefined for a hidden row — then route the five server entry points and the two client lists through them. Those two exports plu

---

## 3. Unverified surfaces from the same sweep (129)

Claimed but never adversarially checked. Treat as leads, not findings.

- `/Users/dan/Documents/ChatGPT Ads/client/public/sitemap.xml:20` · `door linkedin-growth` — The XML comment reads '- The gated doors, today /services/linkedin-growth.' XML comments are served verbatim to anyone who opens /sitemap.xml, so this comment publishes the exact a
- `/Users/dan/Documents/ChatGPT Ads/shared/builds.ts:96` · `LinkedIn automation products Top-Voice and Warmlike` — GENERATED artefact (header line 2: 'GENERATED — do not edit. Run npx tsx scripts/build-builds.ts'). The 'top-voice' row and the 'warmlike' row at line 129 are the data behind both 
- `/Users/dan/Documents/ChatGPT Ads/data/builds/top-voice.json:5` · `LinkedIn automation products Top-Voice and Warmlike` — The generator input: oneLine is 'Organic LinkedIn growth engine that turns targeted reactions into inbound traffic'. Its sibling data/builds/warmlike.json line 5 reads 'A fully man
- `/Users/dan/Documents/ChatGPT Ads/client/public/assets/products/top-voice.png:1` · `door linkedin-growth (its product imagery)` — A tracked, publicly served static image (git ls-files confirms it, and everything under client/public is copied to dist/public). It and client/public/assets/products/warmlike.png a
- `/Users/dan/Documents/ChatGPT Ads/client/public/llms.txt:39` · `door linkedin-automation` — The committed llms.txt names the door in full to any arriving agent: '- [LinkedIn automation — with a written legal assessment](https://top-rated.team/services/linkedin-automation)
- `/Users/dan/Documents/ChatGPT Ads/client/public/llms-full.txt:252` · `door linkedin-automation and agent linkedin-automation` — The long machine index carries the door's entire page as text at lines 252-293: the heading '### 6. LinkedIn automation — with a written legal assessment', the URL, the full blurb 
- `/Users/dan/Documents/ChatGPT Ads/scripts/build-llms.ts:92` · `the hidden mechanism itself` — `const PUBLIC_DOORS = DOORS.filter((door) => !door.hidden && PUBLIC_TIERS.includes(door.tier));` — the only place in the whole tree where the hidden flag reaches a generated artefa
- `/Users/dan/Documents/ChatGPT Ads/client/src/pages/door.tsx:141` · `door linkedin-automation` — The noindex effect is `if (isPublicDoor(door)) return;` — keyed to TIER, not to hidden. linkedin-automation is light-grey, i.e. a public tier, so its page carries no robots meta to
- `/Users/dan/Documents/ChatGPT Ads/scripts/build-social-card.ts:26` · `none — verified` — VERIFIED CLEAN. The 1200x630 card generator's only text is HEADLINE 'Hire a hybrid team / for your business.', MECHANISM 'Digital Experts + any AI agents in one room.' and ADDRESS 
- `/Users/dan/Documents/ChatGPT Ads/shared/doors.ts:626` · `door linkedin-automation and door linkedin-growth` — `export function catalogue(operator?)` returns resolveCatalogue(DOORS, operator) with no hidden filter, and shared/operator.ts builds operatorDoors (line 90) and defaultServices (l
- `/Users/dan/Documents/ChatGPT Ads/docs/case-studies.html:954` · `LinkedIn products and agent linkedin-dev (repo only, not served)` — A generated artefact (scripts/build-cases-pdf.ts) tracked in git, together with docs/case-studies.pdf and docs/prompts.html. It contains the Top-Voice copy — 'LinkedIn visibility p
- `/Users/dan/Documents/ChatGPT Ads/server/connector/handlers.ts:108` · `door linkedin-automation and door linkedin-growth` — listServices() is `DOORS.map(publicService)` — every row, no filter. A machine reads headline, blurb, slug, path, status, tier, contract, agentLine, all four starters and firstAgen
- `/Users/dan/Documents/ChatGPT Ads/server/connector/index.ts:49` · `door linkedin-automation and door linkedin-growth` — GET /api/connector/services — unauthenticated, CORS wide open (Access-Control-Allow-Origin: *, line 32), and the address a Custom GPT imports. Returns the unfiltered list above. An
- `/Users/dan/Documents/ChatGPT Ads/server/ai/route-question.ts:469` · `door linkedin-automation` — fillChoices() falls back to `for (const door of DOORS)` with only `status !== 'live'` and `!firstAgentId` as guards. A hidden live door with an agent is offered as a named button. 
- `/Users/dan/Documents/ChatGPT Ads/client/src/pages/door.tsx:726` · `door linkedin-automation and door linkedin-growth` — A hidden door's address answers HTTP 200 with the SPA shell and renders NotFound client-side — a soft 404. The noindex effect that protects non-public doors lives inside DoorPage (
- `/Users/dan/Documents/ChatGPT Ads/shared/operator.ts:90` · `door linkedin-automation` — operatorDoors() filters only on `contract.legalName === house` — it keeps every door of ours, hidden or not. It is returned as OperatorPublic.doors by GET /api/operator (server/rou
- `/Users/dan/Documents/ChatGPT Ads/client/public/llms-full.txt:254` · `door linkedin-automation and agent linkedin-automation` — The long version carries the whole door entry at this URL, and at line 272 prints 'Panel: The LinkedIn Automation Agent answers first, from LinkedIn's own developer documentation' 
- `/Users/dan/Documents/ChatGPT Ads/scripts/build-llms.ts:323` · `door linkedin-automation and door linkedin-growth` — `DOORS.length > PUBLIC_DOORS.length` prints 'More offers than the N listed. /services asks for an email address before it names them, they are absent from sitemap.xml, and their pa
- `/Users/dan/Documents/ChatGPT Ads/server/connector/handlers.ts:212` · `door linkedin-growth and door linkedin-automation` — createRoom() validates doorId only with `!DOOR_BY_ID[doorId]`. POST /api/connector/rooms with doorId 'linkedin-growth' mints a real room stamped with that door: storage.ts:157 reso
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/home/doorText.ts:36` · `door linkedin-automation` — ANSWERING_DOORS = DOORS.filter(status === 'live' && firstAgentId) — no hidden check. HouseAsk.tsx:46 takes `.slice(0, 3)` for its example questions. linkedin-automation is the sixt
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/CaseFilter.tsx:25` · `door linkedin-automation` — DOORS_WITH_CASES = DOORS.filter(casesForDoor(door.id).length > 0) — no hidden check. The select on /case-studies prints `door.headline` for each (line 84). No linkedin door has a c
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/GatedOffers.tsx:82` · `door linkedin-growth` — PARTNER_DOORS = DOORS.filter((door) => !isPublicDoor(door)) — the one derived list in this file that does NOT check hidden, and it is precisely the list that contains linkedin-grow
- `/Users/dan/Documents/ChatGPT Ads/server/routes.ts:559` · `door linkedin-automation` — `/work/:slug` and `/use-case/:slug` 301 to `/services/:slug` for any slug, hidden or not. A crawler holding /work/linkedin-automation is permanently redirected to the hidden door's
- `/Users/dan/Documents/ChatGPT Ads/shared/roster.ts:9` · `agent linkedin-automation and agent linkedin-dev` — CONFIRMED: AgentDef has no `hidden` field and no equivalent — the file contains zero occurrences of the word, and nothing anywhere filters the AGENTS array. Hiding the LinkedIn Aut
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/doors/PartnerBody.tsx:27` · `door linkedin-growth` — `DOOR_BY_ID['linkedin-growth']` and the PARTNER_BUILDS render (line 62) — the partner door's whole page body, describing top-voice.ai and warmlike.com. Unreachable today because do
- `/Users/dan/Documents/ChatGPT Ads/shared/roster.ts:9` · `mechanism gap: agents` — AgentDef has no visibility field at all — id, handle, name, title, blurb, initials, mark, tone, useKb, kbNamespace, systemPrompt, starters. There is NO agent equivalent of DoorDef.
- `/Users/dan/Documents/ChatGPT Ads/server/bridge/index.ts:44` · `agent:linkedin-automation, agent:linkedin-dev` — A second AGENT_KEYS map, built from every row of AGENTS, used by agentsNamedIn (line 380) and runBridgedAgentTurn (line 595/625). A message arriving over WhatsApp (WAHA) or Chatwoo
- `/Users/dan/Documents/ChatGPT Ads/server/seats.ts:297` · `agent:linkedin-automation, agent:linkedin-dev` — A third mention resolver — agentsNamedIn() iterates AGENTS and remembers handle, id and `agent:<id>`; used at line 380 to record which agents a person named in a message.
- `/Users/dan/Documents/ChatGPT Ads/client/src/pages/workspace.tsx:389` · `agent:linkedin-automation, agent:linkedin-dev` — openAgent(agentId) — the sidebar's click handler. Resolves AGENT_BY_ID, then POSTs a new channel named after the agent's handle with counterpartKey `agent:<id>`. No visibility chec
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/workspace/MessageItem.tsx:40` · `agent:linkedin-automation, agent:linkedin-dev` — Every agent turn in the message column is labelled with the agent's roster name, resolved from AGENT_BY_ID. A room that has one LinkedIn Automation Agent answer in its history keep
- `/Users/dan/Documents/ChatGPT Ads/server/ai/route-question.ts:469` · `door:linkedin-automation, door:linkedin-growth` — uniqueComingDoor() and the token-holder scan at line 505 both iterate the raw DOORS array, so a hidden door's text still participates in routing and can trigger the "that service i
- `/Users/dan/Documents/ChatGPT Ads/server/routes.ts:1202` · `agent:linkedin-automation, door:linkedin-automation` — POST /api/route-question — the public, unauthenticated endpoint the home-page panel calls. It returns the routing result verbatim, including the doorId, the agentId and the reason 
- `/Users/dan/Documents/ChatGPT Ads/shared/pricing.ts:132` · `LinkedIn service, outside the door table` — A published price row on /pricing: price "free", buys "LinkedIn content autopilot, and the AI agents." A LinkedIn automation feature advertised as a headline free offer, on a page 
- `/Users/dan/Documents/ChatGPT Ads/client/public/llms-full.txt:272` · `agent:linkedin-automation, door:linkedin-automation` — Generated and served to machines: "Panel: The LinkedIn Automation Agent answers first, from LinkedIn's own [developer documentation]..." plus the whole door entry at lines 252-290 
- `/Users/dan/Documents/ChatGPT Ads/client/public/sitemap.xml:74` · `door:linkedin-automation` — https://top-rated.team/services/linkedin-automation is handed to crawlers. This file is hand-maintained, not generated — it correctly omits linkedin-growth (the comment at line 20 
- `/Users/dan/Documents/ChatGPT Ads/client/src/pages/landing.tsx:39` · `door:linkedin-automation` — The home page's own meta description, served on / : "...paid advertising and the measurement under it, Google Ad Grants, inbound LinkedIn on the official API, and custom AI around 
- `/Users/dan/Documents/ChatGPT Ads/client/src/pages/door.tsx:725` · `door:linkedin-growth, door:linkedin-automation` — The door route already honours the flag: `const door = found && !found.hidden ? found : undefined;` → NotFound. A hidden door's page and its whole panel, including its agent, are u
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/GatedOffers.tsx:79` · `door:linkedin-automation` — PUBLIC_DOORS = DOORS.filter(!hidden && isPublicDoor) — drives the counts and headlines on /services (doors.tsx:15,25) and the Plate diagram (Plate.tsx:96,102). Honours the flag, so
- `/Users/dan/Documents/ChatGPT Ads/scripts/build-llms.ts:92` · `door:linkedin-growth, door:linkedin-automation` — PUBLIC_DOORS = DOORS.filter(!door.hidden && PUBLIC_TIERS.includes(tier)) — the generator honours the flag, and llms.txt/llms-full.txt are correct for linkedin-growth. It re-derives
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/doors/bodies.tsx:54` · `door:linkedin-growth` — DOOR_BODIES maps "linkedin-growth" to PartnerBody, which renders PARTNER_BUILDS (Top Voice and WarmLike, described at length from their `forPartner` text in shared/builds.ts:114 an
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/CaseFilter.tsx:25` · `door:linkedin-automation, door:linkedin-growth` — DOORS_WITH_CASES = DOORS.filter(casesForDoor(door.id).length > 0) — no hidden filter, so a hidden door would appear as a filter chip on /case-studies if it ever had a case. shared/
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/LeadDialog.tsx:46` · `door:linkedin-automation, door:linkedin-growth` — collectSource() and doorForPath() (line 59) look a door up by window.location.pathname across raw DOORS, with no hidden filter. Harmless while the page 404s, but it means a hidden 
- `/Users/dan/Documents/ChatGPT Ads/client/src/lib/booking.ts:132` · `door:linkedin-automation, door:linkedin-growth` — The booking source is derived from DOORS by pathname with no hidden filter — same shape as LeadDialog, same low exposure.
- `/Users/dan/Documents/ChatGPT Ads/server/spend.ts:234` · `agent:linkedin-dev` — AGENT_CEILINGS names "linkedin-dev" with a $2 / 12-turns-per-hour ceiling. Server-only and never sent to a browser (the comment at line 230 says so deliberately), so it is not a vi
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/workspace/AdmitAgentDialog.tsx:4` · `agent roster, dormant` — An outside-agent admission dialog that imports the roster. It is imported by nothing (verified: no reference outside its own file), so it renders nowhere today. Flagged so it is no
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/doors/AiBuildsBody.tsx:66` · `Top Voice — LinkedIn growth/engagement product` — Rendered as body copy on the PUBLIC /services/ai-builds door page (line 160 maps over BUILDS, line 186 prints shown.what). Visitor reads: "A LinkedIn visibility platform built arou
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/doors/AiBuildsBody.tsx:68` · `Top Voice — LinkedIn growth/engagement product` — Bullet under Top Voice on /services/ai-builds: "A setup that takes four fields and produces an Influence Flow, a content campaign and a boosting campaign."
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/doors/AiBuildsBody.tsx:69` · `Top Voice — LinkedIn growth/engagement product` — Bullet under Top Voice on /services/ai-builds: "Influence Flows: synthesised influencer voices, including custom ones from a LinkedIn profile URL."
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/doors/AiBuildsBody.tsx:70` · `Top Voice — LinkedIn growth/engagement product` — Bullet under Top Voice on /services/ai-builds: "A Content Studio with Autopilot — post copy, images, carousel PDFs and video, published to profiles or company pages on a schedule."
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/doors/AiBuildsBody.tsx:71` · `Top Voice — LinkedIn growth/engagement product` — Bullet under Top Voice on /services/ai-builds: "A LinkedIn Booster: boolean searches that assemble a targeted posts feed, and a reaction engine with human-like pacing and selectabl
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/doors/AiBuildsBody.tsx:72` · `Top Voice — LinkedIn growth/engagement product` — Bullet under Top Voice on /services/ai-builds: "A pool of dedicated in-house LinkedIn accounts, so a campaign can run without the client handing over their own login."
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/doors/AiBuildsBody.tsx:73` · `Top Voice — LinkedIn growth/engagement product` — Bullet under Top Voice on /services/ai-builds: "MCP connectors — Add to ChatGPT and Add to Claude — alongside in-app, web-chat and LinkedIn DM entry points."
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/doors/AiBuildsBody.tsx:77` · `WarmLike — LinkedIn growth/engagement product` — Body copy on /services/ai-builds: "A LinkedIn visibility service around a second, managed company page rather than the client's own account. You tell them the profile and the niche
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/doors/AiBuildsBody.tsx:79` · `WarmLike — LinkedIn growth/engagement product` — Bullet under WarmLike on /services/ai-builds: "The managed company page — a pre-lander — run as the service, not as access to the client's LinkedIn login."
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/doors/AiBuildsBody.tsx:80` · `WarmLike — LinkedIn growth/engagement product` — Bullet under WarmLike on /services/ai-builds: "Daily suggested posts, reviewed and approved from a private link, published to the client's own profile and reshared by that page."
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/doors/AiBuildsBody.tsx:81` · `WarmLike — LinkedIn growth/engagement product` — Bullet under WarmLike on /services/ai-builds: "The public site at warmlike.com, which is how the work is started: a message or a call, not a checkout."
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/doors/AiBuildsBody.tsx:175` · `Top Voice / WarmLike — LinkedIn growth products` — The portfolio row headings and links themselves on /services/ai-builds: "Top Voice" linked to https://top-voice.ai with the host printed under it (line 177), and "WarmLike" linked 
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/GatedOffers.tsx:293` · `LinkedIn engagement products (Top Voice / WarmLike)` — On /services, above the email field, before any gate is passed — visible to a total stranger: "Behind this: two products of ours, and no offers from other companies. What they have
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/GatedOffers.tsx:137` · `Top Voice — LinkedIn engagement product` — Product card on /services after the email step: heading "Top-Voice", link "top-voice.ai", body "Engagement on LinkedIn from profiles you already own — commenting and replying where
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/GatedOffers.tsx:145` · `WarmLike — LinkedIn engagement product` — Product card on /services after the email step: heading "Warmlike", link "warmlike.com", body "The same kind of work pointed at a list you bring: warming up the people on it before
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/GatedOffers.tsx:447` · `door linkedin-automation (cross-reference) + LinkedIn engagement products` — Closing paragraph of the revealed section on /services: "They act on real LinkedIn accounts, and LinkedIn has rules about that which it enforces. Nothing here says otherwise. If yo
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/GatedOffers.tsx:255` · `LinkedIn engagement products` — Not on screen, but sent to the lead inbox and stored with the lead: message "Asked to see the LinkedIn engagement offers on /services." with intent "linkedin-engagement" (line 213)
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/home/FirstScreen.tsx:270` · `LinkedIn service (non-ads) named on the home page` — First screen of the home page, the lead paragraph every visitor reads: "Start with a free expert audit of what the AI agents and automations in your ad accounts have been doing. Th
- `/Users/dan/Documents/ChatGPT Ads/client/src/pages/landing.tsx:39` · `LinkedIn service (non-ads) named in the home page meta description` — The home page <meta name="description">, i.e. what search engines and link previews show: "A free expert read of what the AI agents and automations in your ad accounts have been do
- `/Users/dan/Documents/ChatGPT Ads/shared/pricing.ts:132` · `LinkedIn content autopilot — a non-ads LinkedIn service` — A row of the price ladder, printed on the home page (landing.tsx:116 <Ladder />), on /pricing (pricing.tsx:44) and pre-filled into the /setup form (setup.tsx:432): price "free", wh
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/BookingDialog.tsx:494` · `LinkedIn sign-in integration, advertised while the app is under review` — A visible (disabled) button in the booking popup, which opens from Book-a-call links on the home page, /services, every door page and /pricing: "Sign in with LinkedIn". The comment
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/BookingDialog.tsx:491` · `LinkedIn sign-in integration` — The button's native tooltip, readable on hover: "Not on yet. It turns on when the LinkedIn app is live." It tells a visitor there is a LinkedIn application pending.
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/BookingDialog.tsx:787` · `LinkedIn sign-in integration` — Hint text under the email field in the booking popup: "An address gets you a calendar invite and any reminder. Sign in with LinkedIn does the same without typing it, and is not on 
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/BookingDialog.tsx:789` · `LinkedIn sign-in integration` — The other branch of the same hint: "An address is required — type it, or sign in with LinkedIn when that is on. This page does not take a booking without one."
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/workspace/WorkspaceSidebar.tsx:140` · `agent linkedin-automation and agent linkedin-dev` — Every room's left sidebar builds its Agents list from the whole roster — `AGENTS.filter(...)` at lines 139-140, printed as `{agent.name}` at line 227. Six show by default and a "N 
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/workspace/Composer.tsx:46` · `agent linkedin-automation and agent linkedin-dev` — The @mention autocomplete in every room's composer maps over the whole roster: `AGENTS.map(...)` at line 46, showing `label: agent.name` (line 49) and `detail: agent.title` (line 5
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/workspace/MessageList.tsx:246` · `agent linkedin-automation and agent linkedin-dev` — The empty-state header of an agent channel prints `channelAgent.name` — "LinkedIn Automation Agent" / "LinkedIn Dev Agent" — as the <h2> a visitor sees on opening that channel.
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/workspace/MessageList.tsx:250` · `agent linkedin-automation and agent linkedin-dev` — Directly under it, `channelAgent.blurb`. For linkedin-automation: "Which permissions are open to any developer and which need LinkedIn's approval, and what the interfaces for invit
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/workspace/MessageList.tsx:257` · `agent linkedin-automation` — Under "Ask one of these", the agent's first four starters render as clickable buttons (starterQuestions at line 47-53, buttons at line 257-263). For the LinkedIn Automation Agent t
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/workspace/MemberRail.tsx:272` · `agent linkedin-automation and agent linkedin-dev` — The member rail prints `agent.title` as the agent's role line (roleFor, line 268-275) and again as the hover title (hoverForKey, line 375): "Reads LinkedIn's own developer document
- `/Users/dan/Documents/ChatGPT Ads/shared/roster.ts:528` · `agent linkedin-automation` — The source of the above: name "LinkedIn Automation Agent", title "Reads LinkedIn's own developer documentation" (line 529), blurb (line 531). This row has no way to be marked invis
- `/Users/dan/Documents/ChatGPT Ads/shared/roster.ts:542` · `agent linkedin-automation` — The four starter questions above, as data: "Which LinkedIn permissions can any developer get without approval?", "Can an app send connection invitations through the official API?",
- `/Users/dan/Documents/ChatGPT Ads/shared/roster.ts:911` · `agent linkedin-dev` — name "LinkedIn Dev Agent", title "Designs LinkedIn messaging and calendar integrations" (line 912), blurb (line 913-914). Printed wherever agents are listed.
- `/Users/dan/Documents/ChatGPT Ads/shared/roster.ts:920` · `agent linkedin-dev` — LinkedIn Dev Agent starters, rendered as clickable buttons in its channel: "Can we send LinkedIn messages from our own app, and what has to be connected first?" plus three non-Link
- `/Users/dan/Documents/ChatGPT Ads/shared/roster.ts:706` · `AI Lawyer Agent — LinkedIn automation prose in every room` — THE WORST ONE, because the AI Lawyer Agent is seeded into EVERY room of ours (shared/playbook.ts:203-217 puts the #ask-legal channel in every non-partner room). Its first three sta
- `/Users/dan/Documents/ChatGPT Ads/shared/roster.ts:752` · `agent linkedin-automation, named by another agent` — Inside the AI Lawyer Agent's system prompt, i.e. it shapes the text a visitor is shown in an answer: "...That is LinkedIn's own API documentation and the LinkedIn Automation Agent 
- `/Users/dan/Documents/ChatGPT Ads/shared/roster.ts:977` · `agent linkedin-automation, named by agent linkedin-dev` — Same pattern in the LinkedIn Dev Agent's prompt: "You never say whether a particular LinkedIn automation is permitted. That is the lawyer's written assessment. Name @legal and @lin
- `/Users/dan/Documents/ChatGPT Ads/shared/doors.ts:430` · `door linkedin-automation` — The linkedin-automation door row's own prose, printed on /services/linkedin-automation by door.tsx: headline "LinkedIn automation — with a written legal assessment" (line 430), blu
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/home/doorText.ts:36` · `door linkedin-automation (latent)` — ANSWERING_DOORS = DOORS.filter(door => door.status === "live" && door.firstAgentId) — NO hidden check. HouseAsk.tsx:46 takes the first three of these and prints their first starter
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/CaseFilter.tsx:25` · `door linkedin-automation (latent)` — DOORS_WITH_CASES = DOORS.filter(door => casesForDoor(door.id).length > 0) — no hidden check. The select on /case-studies prints `door.headline` as an option label and countLine (li
- `/Users/dan/Documents/ChatGPT Ads/client/public/llms.txt:39` · `door linkedin-automation` — Checked-in generated file served at /llms.txt: "- [LinkedIn automation — with a written legal assessment](https://top-rated.team/services/linkedin-automation): What is and is not a
- `/Users/dan/Documents/ChatGPT Ads/client/public/llms-full.txt:252` · `door linkedin-automation + agent linkedin-automation` — Checked-in generated file served at /llms-full.txt: an entire section "### 6. LinkedIn automation — with a written legal assessment" at https://top-rated.team/services/linkedin-aut
- `/Users/dan/Documents/ChatGPT Ads/client/public/sitemap.xml:74` · `door linkedin-automation` — Checked-in sitemap served at /sitemap.xml lists <loc>https://top-rated.team/services/linkedin-automation</loc>, inviting crawlers to a page that will start 404-ing. The header comm
- `/Users/dan/Documents/ChatGPT Ads/scripts/build-cases-pdf.ts:239` · `LinkedIn growth products, in a client-facing document` — Hardcoded lead-in for the client-facing case-studies document: "The cases above are advertising accounts. The work on the other side of this company — LinkedIn, automation, custom 
- `/Users/dan/Documents/ChatGPT Ads/docs/case-studies.html:954` · `Top Voice / WarmLike — LinkedIn growth products` — The generated 93 KB document that becomes the case-studies PDF handed to prospects. It carries the full unredacted Top Voice text from shared/builds.ts: "Top Voice is a LinkedIn vi
- `/Users/dan/Documents/ChatGPT Ads/shared/builds.ts:99` · `Top Voice / WarmLike — LinkedIn growth products` — The source prose the PDF prints. oneLine "Organic LinkedIn growth engine that turns targeted reactions into inbound traffic", what (line 100), built (lines 103-111), forPartner (li
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/doors/PartnerBody.tsx:57` · `door linkedin-growth` — The linkedin-growth door's body: heading "Two products, on their own sites." and "LinkedIn growth on this door runs on these two. What each one does is taken from that product's ow
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/workspace/IdentifyStrip.tsx:128` · `LinkedIn sign-in integration` — Inside a room: a link labelled "Bind with LinkedIn", with the line at 28-29/146 "This opens LinkedIn's own sign-in. We keep an id and the name on your profile. LinkedIn does not en
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/site/doors/AdGrantDoor.tsx:47` · `LinkedIn sign-in integration (adgrant.ai)` — On the public /services/ad-grants door page, a numbered block titled "LinkedIn, and only for the cap" with body "Sign-in exists only to limit free generations. It reads only the pr
- `/Users/dan/Documents/ChatGPT Ads/client/src/pages/privacy.tsx:146` · `LinkedIn page-management capability described in the privacy policy` — Borderline, and the brief exempts legal/privacy language about the official API — flagged only so the call is deliberate. The "LinkedIn, in detail" section says the service "reads 
- `/Users/dan/Documents/ChatGPT Ads/server/connector/handlers.ts:108` · `door linkedin-automation + door linkedin-growth (already-hidden, incomplete)` — listServices() returns DOORS.map(publicService) with no hidden filter. Every door row leaves the process with headline, blurb, agentLine, all starters, firstAgentId, contract (lega
- `/Users/dan/Documents/ChatGPT Ads/server/connector/index.ts:48` · `door linkedin-automation + door linkedin-growth` — GET /api/connector/services - unauthenticated, no rate limit, and allowCrossOrigin (line 31) sets Access-Control-Allow-Origin: *. Any browser or crawler that fetches this URL gets 
- `/Users/dan/Documents/ChatGPT Ads/server/connector/mcp.ts:36` · `door linkedin-automation, agent linkedin-automation` — MCP tool 'list_services', described to the model as 'List every offer Top-Rated Team publishes: headline, who invoices, whether the panel is open, and which agent answers first. Qu
- `/Users/dan/Documents/ChatGPT Ads/server/connector/openapi.ts:25` · `door linkedin-automation` — The GPT Action operation list_services, whose own description (line 30) promises 'Returns every offer in shared/doors.ts ... Quote the fields as returned.' A custom GPT that import
- `/Users/dan/Documents/ChatGPT Ads/server/connector/handlers.ts:146` · `agent linkedin-automation, agent linkedin-dev` — askQuestion accepts any agentId present in AGENT_BY_ID. POST /api/connector/ask {"agentId":"linkedin-automation"} or {"agentId":"linkedin-dev"} returns a full grounded answer. Unau
- `/Users/dan/Documents/ChatGPT Ads/server/routes.ts:1001` · `agent linkedin-automation, agent linkedin-dev` — POST /api/ask resolves agentId straight out of AGENT_BY_ID with only an existence check. The site's own public ask endpoint will stream a LinkedIn Automation Agent or LinkedIn Dev 
- `/Users/dan/Documents/ChatGPT Ads/server/ai/route-question.ts:246` · `door linkedin-automation, agent linkedin-automation` — scoreDoors builds its index from all of DOORS, filtering only on status. doorDocument (line 179) feeds id, slug, headline, blurb, agentLine and every starter into the scorer, so th
- `/Users/dan/Documents/ChatGPT Ads/server/ai/route-question.ts:463` · `door linkedin-automation` — fillChoices skips only non-live doors, so linkedin-automation is offered as a pick chip labelled with liveChoice(door) = door.headline, i.e. the visitor reads the literal string 'L
- `/Users/dan/Documents/ChatGPT Ads/server/ai/route-question.ts:513` · `door linkedin-automation, agent linkedin-automation` — pickedLiveDoor honours any doorId the caller sends as long as the row is status 'live'. A visitor (or a script) POSTing {"question":"...","pickedDoorId":"linkedin-automation"} gets
- `/Users/dan/Documents/ChatGPT Ads/server/ai/route-question.ts:505` · `door linkedin-growth (already hidden - verification item)` — uniqueComingDoor scans all of DOORS including hidden rows. A question containing a token unique to the hidden linkedin-growth row (e.g. 'Maksymenko') routes to CONTACT_COMING. The 
- `/Users/dan/Documents/ChatGPT Ads/shared/roster.ts:9` · `agent linkedin-automation, agent linkedin-dev (mechanism gap)` — AgentDef has no `hidden` field. Confirmed by reading the whole interface (lines 9-42) and by grep: the only occurrences of 'hidden' in this file are zero. There is no mechanism for
- `/Users/dan/Documents/ChatGPT Ads/shared/roster.ts:526` · `agent linkedin-automation` — The linkedin-automation agent row itself: name 'LinkedIn Automation Agent', title 'Reads LinkedIn's own developer documentation', a blurb about invitations/messages/connections/pos
- `/Users/dan/Documents/ChatGPT Ads/shared/roster.ts:909` · `agent linkedin-dev` — The linkedin-dev agent row: name 'LinkedIn Dev Agent', title 'Designs LinkedIn messaging and calendar integrations', a blurb naming LinkedIn messaging and account connection, and s
- `/Users/dan/Documents/ChatGPT Ads/shared/roster.ts:752` · `agent linkedin-automation (leaked by a kept agent)` — The AI Lawyer Agent's system prompt instructs it: 'That is LinkedIn's own API documentation and the LinkedIn Automation Agent reads it - @linkedin-automation in a room.' The lawyer
- `/Users/dan/Documents/ChatGPT Ads/shared/roster.ts:706` · `adjacent - the kept legal agent advertising LinkedIn automation subject matter` — The AI Lawyer Agent's starter chips, rendered in the ask-legal channel of every room: 'Are automation tools and browser extensions allowed on LinkedIn?' (706), 'Is it against Linke
- `/Users/dan/Documents/ChatGPT Ads/shared/roster.ts:978` · `agent linkedin-automation (cross-reference)` — The LinkedIn Dev Agent's prompt names '@legal and @linkedin-automation' as the handoff for the permission half. Moot while both agents are hidden together, but it becomes a danglin
- `/Users/dan/Documents/ChatGPT Ads/shared/roster.ts:172` · `adjacent - the kept legal agent's self-description` — LEGAL_KB_RULES tells the always-seeded lawyer that its documentation is "the platforms' and regulators' OWN published documents - LinkedIn's User Agreement, Professional Community 
- `/Users/dan/Documents/ChatGPT Ads/data/legal-sources.json:7` · `adjacent - kept legal corpus` — Five LinkedIn documents in the legal corpus source list: User Agreement (7), Professional Community Policies (11), API Terms of Use (15), Privacy Policy (19), 'Prohibited software 
- `/Users/dan/Documents/ChatGPT Ads/scripts/build-kb.ts:377` · `corpus linkedin-automation, orphaned by hiding agent linkedin-automation` — The LINKEDIN_AUTOMATION corpus definition - 40 URLs on learn.microsoft.com/linkedin covering LinkedIn's OAuth flows, Invitations API, Messages API, Connections API, Compliance APIs
- `/Users/dan/Documents/ChatGPT Ads/scripts/build-kb.ts:772` · `corpus unipile-api, orphaned by hiding agent linkedin-dev` — The UNIPILE_API corpus definition - 45 URLs on developer.unipile.com covering LinkedIn account connection, hosted auth, chats, messages and webhooks. Enrolled in CORPORA at line 83
- `/Users/dan/Documents/ChatGPT Ads/scripts/build-kb.ts:831` · `corpus linkedin-automation + corpus unipile-api` — export const CORPORA - the single array that enrols all twelve corpora. It has no notion of a corpus being off, so both LinkedIn orphans are re-fetched by npm run kb:fetch, re-embe
- `/Users/dan/Documents/ChatGPT Ads/data/kb/kb.linkedin-automation.json:1` · `corpus linkedin-automation` — 397 KB, 40 documents, 436 chunks of LinkedIn's own API documentation, committed to git. The .gitignore comment (line 12) states this repository 'is going public'. A published repo 
- `/Users/dan/Documents/ChatGPT Ads/data/kb/kb.unipile-api.json:1` · `corpus unipile-api` — 1.76 MB, 45 documents, 261 chunks of developer.unipile.com - the largest file in data/kb. Committed to git. It names the LinkedIn messaging vendor in every URL and title, in the sa
- `/Users/dan/Documents/ChatGPT Ads/server/ai/kb.ts:326` · `corpus linkedin-automation + corpus unipile-api` — load() indexes every *.json in data/kb that is not an embeddings file. There is no allow-list and no cross-check against the roster, so kb.linkedin-automation.json and kb.unipile-a
- `/Users/dan/Documents/ChatGPT Ads/render.yaml:37` · `corpus linkedin-automation + corpus unipile-api` — buildCommand runs `npm run kb:build` on every push. While the LinkedIn API application is under review, each deploy re-downloads 40 pages of learn.microsoft.com/linkedin and 45 pag
- `/Users/dan/Documents/ChatGPT Ads/client/public/sitemap.xml:74` · `door linkedin-automation` — <loc>https://top-rated.team/services/linkedin-automation</loc>, handed to every crawler. This file is NOT generated - its own header comment (lines 5-13) admits 'A static file cann
- `/Users/dan/Documents/ChatGPT Ads/client/public/llms.txt:39` · `door linkedin-automation` — '[LinkedIn automation - with a written legal assessment](https://top-rated.team/services/linkedin-automation): ...' - the committed build artifact. build-llms.ts:92 DOES filter hid
- `/Users/dan/Documents/ChatGPT Ads/client/public/llms-full.txt:252` · `door linkedin-automation, agent linkedin-automation` — Section '### 6. LinkedIn automation - with a written legal assessment' running to line 289: the full blurb, the agentLine naming the LinkedIn Automation Agent and its refusal, and 
- `/Users/dan/Documents/ChatGPT Ads/shared/operator.ts:90` · `door linkedin-automation` — operatorDoors() maps every door whose contract legalName matches the house, with no hidden filter, into {id, headline, canWhiteLabel}. It reaches GET /api/operator through publicOp
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/workspace/Composer.tsx:46` · `agent linkedin-automation, agent linkedin-dev` — The @mention autocomplete maps all of AGENTS with no filter. Typing '@link' in any room offers 'LinkedIn Ads Agent', 'LinkedIn Automation Agent' and 'LinkedIn Dev Agent' side by si
- `/Users/dan/Documents/ChatGPT Ads/client/src/components/workspace/WorkspaceSidebar.tsx:139` · `agent linkedin-automation, agent linkedin-dev` — The room sidebar splits all of AGENTS into 'here' (139) and 'elsewhere' (140) with no filter, then prints the overflow behind an 'N more' control (line 239). Both hidden LinkedIn a
- `/Users/dan/Documents/ChatGPT Ads/server/routes.ts:206` · `agent linkedin-automation, agent linkedin-dev` — AGENT_KEYS is built from every row in AGENTS, registering both the handle and the id. resolveAgent (line 218) then answers any '@linkedin-automation' or '@linkedin-dev' typed into 
- `/Users/dan/Documents/ChatGPT Ads/server/bridge/index.ts:44` · `agent linkedin-automation, agent linkedin-dev` — The bridge builds its own AGENT_KEYS from all of AGENTS, and agentsNamedIn (line 380) fires a turn for every agent handle found in an inbound WhatsApp, Slack, ChatWoot or ClickUp m
- `/Users/dan/Documents/ChatGPT Ads/server/ai/kb.test.ts:176` · `consequence of hiding agent linkedin-automation, agent linkedin-dev` — Not a visitor surface - a tripwire that will fire during the fix. The test 'every grounded agent names a corpus that exists' iterates AGENTS and asserts hasNamespace(agent.kbNamesp
