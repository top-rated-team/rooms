This directory exists ahead of its code so `scripts/check-parcels.mjs` can verify that the
`adgrant-generate` parcel owns a path that is really there.

What lands here: the half of "generate and upload" that needs no approval from Google — a
generator, a policy gate and a durable count of three. Read
`docs/specs/adgrant-and-dev-agents.md` section 2 first; it separates the two products the
owner's sentence contains and explains why the upload half is a different project.

The policy gate is the point. A structure Google would suspend is worse than no structure,
and the nonprofit carries that risk rather than us.
